openapi: 3.1.0
info: { title: Nanomedicine & Micro-Robotics API, version: 1.0.0 }
paths:
    /nanomedicine/nanomedicine_main:
      post:
        summary: nanomedicine_main
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
    /nanomedicine/nanomedicine_list:
      post:
        summary: nanomedicine_list
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
components:
  schemas:
    Payload:
      type: object
      required: [tenant_id]
      properties:
        tenant_id: {type: string, format: uuid}
        patient_id: {type: string, format: uuid}
