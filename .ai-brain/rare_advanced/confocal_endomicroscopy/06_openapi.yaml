openapi: 3.1.0
info: { title: Confocal Laser Endomicroscopy API, version: 1.0.0 }
paths:
    /confocal_endomicroscopy/confocal_endomicrosc_main:
      post:
        summary: confocal_endomicrosc_main
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
    /confocal_endomicroscopy/confocal_endomicrosc_list:
      post:
        summary: confocal_endomicrosc_list
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
components:
  schemas:
    Payload:
      type: object
      required: [tenant_id]
      properties:
        tenant_id: {type: string, format: uuid}
        patient_id: {type: string, format: uuid}
