openapi: 3.1.0
info: { title: Pharmacogenomics API, version: 1.0.0 }
paths:
    /pharmacogenomics/pharmacogenomics_main:
      post:
        summary: pharmacogenomics_main
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
    /pharmacogenomics/pharmacogenomics_list:
      post:
        summary: pharmacogenomics_list
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
components:
  schemas:
    Payload:
      type: object
      required: [tenant_id]
      properties:
        tenant_id: {type: string, format: uuid}
        patient_id: {type: string, format: uuid}
