openapi: 3.1.0
info: { title: Nuclear Medicine Therapy API, version: 1.0.0 }
paths:
    /nuclear_therapy/nuclear_therapy_main:
      post:
        summary: nuclear_therapy_main
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
    /nuclear_therapy/nuclear_therapy_list:
      post:
        summary: nuclear_therapy_list
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
components:
  schemas:
    Payload:
      type: object
      required: [tenant_id]
      properties:
        tenant_id: {type: string, format: uuid}
        patient_id: {type: string, format: uuid}
