openapi: 3.1.0
info: { title: Cryotherapy Unit API, version: 1.0.0 }
paths:
    /cryosurgery_unit/cryosurgery_unit_main:
      post:
        summary: cryosurgery_unit_main
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
    /cryosurgery_unit/cryosurgery_unit_list:
      post:
        summary: cryosurgery_unit_list
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
components:
  schemas:
    Payload:
      type: object
      required: [tenant_id]
      properties:
        tenant_id: {type: string, format: uuid}
        patient_id: {type: string, format: uuid}
