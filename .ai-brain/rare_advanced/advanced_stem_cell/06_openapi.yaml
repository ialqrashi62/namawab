openapi: 3.1.0
info: { title: Advanced Stem Cell Therapy API, version: 1.0.0 }
paths:
    /advanced_stem_cell/advanced_stem_cell_main:
      post:
        summary: advanced_stem_cell_main
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
    /advanced_stem_cell/advanced_stem_cell_list:
      post:
        summary: advanced_stem_cell_list
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
components:
  schemas:
    Payload:
      type: object
      required: [tenant_id]
      properties:
        tenant_id: {type: string, format: uuid}
        patient_id: {type: string, format: uuid}
