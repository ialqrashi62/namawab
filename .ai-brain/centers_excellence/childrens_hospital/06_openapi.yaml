openapi: 3.1.0
info: { title: Children's Hospital API, version: 1.0.0 }
paths:
    /childrens_hospital/childrens_hospital_main:
      post:
        summary: childrens_hospital_main
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
    /childrens_hospital/childrens_hospital_list:
      post:
        summary: childrens_hospital_list
        requestBody:
          required: true
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Payload'}
        responses:
          '200': {description: OK}
          '400': {description: Validation}
components:
  schemas:
    Payload:
      type: object
      required: [tenant_id]
      properties:
        tenant_id: {type: string, format: uuid}
        patient_id: {type: string, format: uuid}
