import os
MODULES = ['pcc_neuro_ext69', 'pcc_pediatric_neuro_ext58', 'pcc_pediatric_surg_ext58']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T12:10:00Z"

FUNCS = {
    'pcc_neuro_ext69': [
        ('HeadacheClassificationExt', 'const score = Math.round((0.3+Number(input.frequency||3)*0.04)*100)/100;'),
        ('MigraineProphylaxisExt', 'const score = Math.round((0.4+Number(input.headache_days||10)*0.02)*100)/100;'),
        ('ClusterHeadacheExt', 'const score = Math.round((0.5+Number(input.attacks_per_day||3)*0.08)*100)/100;'),
        ('TensionHeadacheExt', 'const score = Math.round((0.25+Number(input.tenderness_score||3)*0.05)*100)/100;'),
        ('TrigeminalNeuralgiaExt', 'const score = Math.round((0.4+Number(input.paroxysms||10)*0.03)*100)/100;'),
        ('MedicationOveruseHeadacheExt', 'const score = Math.round((0.45+Number(input.days_per_month||15)*0.02)*100)/100;'),
        ('ThunderclapHeadacheExt', 'const score = Math.round((0.5+Number(input.severity_peak||10)*0.04)*100)/100;'),
        ('CervicogenicHeadacheExt', 'const score = Math.round((0.3+Number(input.cervical_tender||2)*0.08)*100)/100;'),
        ('PostConcussionHeadacheExt', 'const score = Math.round((0.35+Number(input.pcss_score||20)*0.02)*100)/100;'),
        ('IdiopathicIntracranialHypertensionHeadacheExt', 'const score = Math.round((0.4+Number(input.opening_pressure||25)*0.012)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext58': [
        ('PediatricHeadacheExt', 'const score = Math.round((0.3+Number(input.frequency||2)*0.04)*100)/100;'),
        ('PediatricMigraineExt', 'const score = Math.round((0.4+Number(input.headache_days||5)*0.03)*100)/100;'),
        ('PediatricClusterHeadacheExt', 'const score = Math.round((0.45+Number(input.attacks||1)*0.1)*100)/100;'),
        ('PediatricTensionHeadacheExt', 'const score = Math.round((0.25+Number(input.stress_score||3)*0.05)*100)/100;'),
        ('PediatricTrigeminalExt', 'const score = Math.round((0.4+Number(input.paroxysms||5)*0.04)*100)/100;'),
        ('PediatricMOHExt', 'const score = Math.round((0.4+Number(input.days_per_month||10)*0.03)*100)/100;'),
        ('PediatricThunderclapHeadacheExt', 'const score = Math.round((0.5+Number(input.severity_peak||10)*0.04)*100)/100;'),
        ('PediatricCervicogenicExt', 'const score = Math.round((0.3+Number(input.tender_points||2)*0.08)*100)/100;'),
        ('PediatricPostConcussionExt', 'const score = Math.round((0.35+Number(input.pcss_score||15)*0.025)*100)/100;'),
        ('PediatricIIHHeadacheExt', 'const score = Math.round((0.4+Number(input.opening_pressure||20)*0.014)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext58': [
        ('PediatricTrigeminalSurgeryExt', 'const score = Math.round((0.4+Number(input.vessel_contact||0)*0.15)*100)/100;'),
        ('PediatricMVDExt', 'const score = Math.round((0.5+Number(input.vein_size||3)*0.05)*100)/100;'),
        ('PediatricGammaKnifeHeadacheExt', 'const score = Math.round((0.4+Number(input.dose_g||70)*0.005)*100)/100;'),
        ('PediatricClusterSurgeryExt', 'const score = Math.round((0.5+Number(input.spheno_palatine||0)*0.15)*100)/100;'),
        ('PediatricMigraineSurgeryExt', 'const score = Math.round((0.3+Number(input.trigger_sites||2)*0.08)*100)/100;'),
        ('PediatricOccipitalStimExt', 'const score = Math.round((0.35+Number(input.leads||2)*0.06)*100)/100;'),
        ('PediatricVCNSSurgeryExt', 'const score = Math.round((0.4+Number(input.intensity||2)*0.08)*100)/100;'),
        ('PediatricBotoxInjectionExt', 'const score = Math.round((0.3+Number(input.sites||5)*0.04)*100)/100;'),
        ('PediatricHeadacheBlockExt', 'const score = Math.round((0.25+Number(input.nerves||3)*0.05)*100)/100;'),
        ('PediatricPNSMigraineExt', 'const score = Math.round((0.3+Number(input.leads||4)*0.04)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3gz.py -- PCC v3.168.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.168.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.168.0, P3-GZ)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.168.0 / P3-GZ']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3gz_v3_168_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-GZ 3.168.0: {count} files written")
