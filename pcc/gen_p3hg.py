import os
MODULES = ['pcc_neuro_ext76', 'pcc_pediatric_neuro_ext65', 'pcc_pediatric_surg_ext65']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T13:20:00Z"

FUNCS = {
    'pcc_neuro_ext76': [
        ('VertigoDisorderExt', 'const score = Math.round((0.3+Number(input.episodes||3)*0.07)*100)/100;'),
        ('BPPVExt', 'const score = Math.round((0.3+Number(input.dix_positive||0)*0.2)*100)/100;'),
        ('VestibularNeuritisExt', 'const score = Math.round((0.4+Number(input.caloric_asym||30)*0.01)*100)/100;'),
        ('MeniereDiseaseExt', 'const score = Math.round((0.4+Number(input.crises||3)*0.08)*100)/100;'),
        ('AcousticNeuromaExt', 'const score = Math.round((0.4+Number(input.tumor_size||10)*0.03)*100)/100;'),
        ('VestibularMigraineExt', 'const score = Math.round((0.35+Number(input.headache_days||5)*0.05)*100)/100;'),
        ('MotionSicknessExt', 'const score = Math.round((0.3+Number(input.score||3)*0.08)*100)/100;'),
        ('BilateralVestibularExt', 'const score = Math.round((0.4+Number(input.loss_pct||50)*0.01)*100)/100;'),
        ('VEMPTestExt', 'const score = Math.round((0.4+Number(input.amplitude_asym||30)*0.01)*100)/100;'),
        ('OcularMotorExamExt', 'const score = Math.round((0.35+Number(input.nystagmus_score||2)*0.1)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext65': [
        ('PediatricVertigoExt', 'const score = Math.round((0.3+Number(input.episodes||2)*0.08)*100)/100;'),
        ('PediatricBPPVExt', 'const score = Math.round((0.3+Number(input.dix_positive||0)*0.2)*100)/100;'),
        ('PediatricVestibularNeuritisExt', 'const score = Math.round((0.4+Number(input.caloric_asym||25)*0.012)*100)/100;'),
        ('PediatricMeniereExt', 'const score = Math.round((0.4+Number(input.crises||1)*0.2)*100)/100;'),
        ('PediatricAcousticNeuromaExt', 'const score = Math.round((0.4+Number(input.tumor_size||5)*0.05)*100)/100;'),
        ('PediatricVestibularMigraineExt', 'const score = Math.round((0.35+Number(input.headache_days||3)*0.07)*100)/100;'),
        ('PediatricMotionSicknessExt', 'const score = Math.round((0.3+Number(input.score||2)*0.1)*100)/100;'),
        ('PediatricBilateralVestibExt', 'const score = Math.round((0.4+Number(input.loss_pct||30)*0.015)*100)/100;'),
        ('PediatricVEMPTestExt', 'const score = Math.round((0.4+Number(input.amplitude_asym||20)*0.015)*100)/100;'),
        ('PediatricOcularMotorExt', 'const score = Math.round((0.35+Number(input.nystagmus_score||1)*0.15)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext65': [
        ('PediatricVestibularSurgeryExt', 'const score = Math.round((0.4+Number(input.procedure_count||1)*0.1)*100)/100;'),
        ('PediatricLabyrinthectomyExt', 'const score = Math.round((0.5+Number(input.hearing_loss||30)*0.01)*100)/100;'),
        ('PediatricEndolymphaticShuntExt', 'const score = Math.round((0.4+Number(input.crises_freq||3)*0.08)*100)/100;'),
        ('PediatricAcousticNeuromaResectExt', 'const score = Math.round((0.5+Number(input.size_mm||15)*0.02)*100)/100;'),
        ('PediatricRetrosigmoidApproachExt', 'const score = Math.round((0.4+Number(input.deficits||1)*0.15)*100)/100;'),
        ('PediatricMiddleFossaApproachExt', 'const score = Math.round((0.4+Number(input.hearing_preserved||0)*0.2)*100)/100;'),
        ('PediatricVestibularNerveSectionExt', 'const score = Math.round((0.5+Number(input.crises||3)*0.1)*100)/100;'),
        ('PediatricHearingRehabExt', 'const score = Math.round((0.3+Number(input.device_count||1)*0.15)*100)/100;'),
        ('PediatricBalanceTherapyExt', 'const score = Math.round((0.3+Number(input.sessions||10)*0.04)*100)/100;'),
        ('PediatricPositionalTrainingExt', 'const score = Math.round((0.3+Number(input.sessions||5)*0.08)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3hg.py -- PCC v3.175.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.175.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.175.0, P3-HG)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.175.0 / P3-HG']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3hg_v3_175_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-HG 3.175.0: {count} files written")
