// Auto-generated by gen_p3master.py -- PCC 3.192.0
"use strict";
const TS = '2026-07-29T14:00:00Z';
const VER = '3.192.0';

function RFAssessmentExt(input) {
  const score = Math.round((0.3 + (Number(input.rfas) || 0) * 0.15) * 100) / 100;
  return { version: VER, module: 'pcc_respiratory_failure', function: 'RFAssessmentExt', input, score, ts: TS };
}

function RFScoreExt(input) {
  const score = Math.round((0.3 + (Number(input.rfsc) || 0) * 0.26) * 100) / 100;
  return { version: VER, module: 'pcc_respiratory_failure', function: 'RFScoreExt', input, score, ts: TS };
}

function RFStageExt(input) {
  const score = Math.round((0.3 + (Number(input.rfst) || 0) * 0.27) * 100) / 100;
  return { version: VER, module: 'pcc_respiratory_failure', function: 'RFStageExt', input, score, ts: TS };
}

function RFPlanExt(input) {
  const score = Math.round((0.3 + (Number(input.rfpl) || 0) * 0.23) * 100) / 100;
  return { version: VER, module: 'pcc_respiratory_failure', function: 'RFPlanExt', input, score, ts: TS };
}

function RFRiskExt(input) {
  const score = Math.round((0.3 + (Number(input.rfri) || 0) * 0.24) * 100) / 100;
  return { version: VER, module: 'pcc_respiratory_failure', function: 'RFRiskExt', input, score, ts: TS };
}

function RFDoseExt(input) {
  const score = Math.round((0.3 + (Number(input.rfdo) || 0) * 0.25) * 100) / 100;
  return { version: VER, module: 'pcc_respiratory_failure', function: 'RFDoseExt', input, score, ts: TS };
}

function RFFrequencyExt(input) {
  const score = Math.round((0.3 + (Number(input.rffr) || 0) * 0.16) * 100) / 100;
  return { version: VER, module: 'pcc_respiratory_failure', function: 'RFFrequencyExt', input, score, ts: TS };
}

function RFDurationExt(input) {
  const score = Math.round((0.3 + (Number(input.rfdu) || 0) * 0.47) * 100) / 100;
  return { version: VER, module: 'pcc_respiratory_failure', function: 'RFDurationExt', input, score, ts: TS };
}

function RFFollowupExt(input) {
  const score = Math.round((0.3 + (Number(input.rffo) || 0) * 0.48) * 100) / 100;
  return { version: VER, module: 'pcc_respiratory_failure', function: 'RFFollowupExt', input, score, ts: TS };
}

function RFOutcomeExt(input) {
  const score = Math.round((0.3 + (Number(input.rfou) || 0) * 0.44) * 100) / 100;
  return { version: VER, module: 'pcc_respiratory_failure', function: 'RFOutcomeExt', input, score, ts: TS };
}

module.exports = {
  RFAssessmentExt,
  RFScoreExt,
  RFStageExt,
  RFPlanExt,
  RFRiskExt,
  RFDoseExt,
  RFFrequencyExt,
  RFDurationExt,
  RFFollowupExt,
  RFOutcomeExt,
};