import os
MODULES = ['pcc_neuro_ext82', 'pcc_pediatric_neuro_ext71', 'pcc_pediatric_surg_ext71']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T14:20:00Z"

FUNCS = {
    'pcc_neuro_ext82': [
        ('HuntingtonDiseaseExt', 'const score = Math.round((0.4+Number(input.repeats||40)*0.005)*100)/100;'),
        ('HDEyeTrackerExt', 'const score = Math.round((0.4+Number(input.saccade_score||3)*0.08)*100)/100;'),
        ('HDFunctionalExt', 'const score = Math.round((0.4+Number(input.tfc_score||7)*0.05)*100)/100;'),
        ('HDNeuropsychExt', 'const score = Math.round((0.4+Number(input.cog_score||20)*0.02)*100)/100;'),
        ('HDImagingExt', 'const score = Math.round((0.4+Number(input.caudate_ratio||0.5)*0.6)*100)/100;'),
        ('HDGeneticTestingExt', 'const score = Math.round((0.4+Number(input.repeats||40)*0.005)*100)/100;'),
        ('HDAntidopaminergicExt', 'const score = Math.round((0.4+Number(input.doses||2)*0.15)*100)/100;'),
        ('HDSRP14003Ext', 'const score = Math.round((0.4+Number(input.dose_mg||120)*0.003)*100)/100;'),
        ('HDChoreaTreatmentExt', 'const score = Math.round((0.4+Number(input.chorea_score||3)*0.1)*100)/100;'),
        ('HDBehavioralMgmtExt', 'const score = Math.round((0.3+Number(input.behavioral_score||3)*0.1)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext71': [
        ('PediatricHuntingtonExt', 'const score = Math.round((0.4+Number(input.repeats||40)*0.005)*100)/100;'),
        ('PediatricJuvenileHDExt', 'const score = Math.round((0.4+Number(input.repeats||60)*0.003)*100)/100;'),
        ('PediatricHDLBDegExt', 'const score = Math.round((0.4+Number(input.age_of_onset||10)*0.02)*100)/100;'),
        ('PediatricHDNeuroExt', 'const score = Math.round((0.4+Number(input.cog_score||15)*0.025)*100)/100;'),
        ('PediatricHDEyeTrackExt', 'const score = Math.round((0.4+Number(input.saccade||2)*0.1)*100)/100;'),
        ('PediatricHDTFCScoreExt', 'const score = Math.round((0.4+Number(input.tfc||5)*0.07)*100)/100;'),
        ('PediatricHDBaselineExt', 'const score = Math.round((0.3+Number(input.baseline_age||10)*0.04)*100)/100;'),
        ('PediatricHDFamilyHxExt', 'const score = Math.round((0.3+Number(input.affected_parents||1)*0.15)*100)/100;'),
        ('PediatricHDGeneticCounselExt', 'const score = Math.round((0.3+Number(input.sessions||2)*0.15)*100)/100;'),
        ('PediatricHDBehavioralExt', 'const score = Math.round((0.3+Number(input.behavior_score||3)*0.1)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext71': [
        ('PediatricDBSChoreaExt', 'const score = Math.round((0.5+Number(input.chorea_score||3)*0.1)*100)/100;'),
        ('PediatricDBSDystoniaExt', 'const score = Math.round((0.5+Number(input.dystonia_score||3)*0.1)*100)/100;'),
        ('PediatricDBSHDGpiExt', 'const score = Math.round((0.4+Number(input.gpi_score||3)*0.08)*100)/100;'),
        ('PediatricDeepBrainStimTrialExt', 'const score = Math.round((0.4+Number(input.eligibility||0)*0.2)*100)/100;'),
        ('PediatricGeneTherapyExt', 'const score = Math.round((0.45+Number(input.trial_count||1)*0.15)*100)/100;'),
        ('PediatricASOSTrialExt', 'const score = Math.round((0.4+Number(input.dose_mg||100)*0.003)*100)/100;'),
        ('PediatricASHLExt', 'const score = Math.round((0.4+Number(input.dose_mg||90)*0.003)*100)/100;'),
        ('PediatricPDE10Ext', 'const score = Math.round((0.4+Number(input.dose_mg||15)*0.02)*100)/100;'),
        ('PediatricNeuropsychTestingExt', 'const score = Math.round((0.3+Number(input.testing_hours||5)*0.06)*100)/100;'),
        ('PediatricOccupationalTherapyExt', 'const score = Math.round((0.3+Number(input.sessions||10)*0.04)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3ia.py -- PCC v3.181.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.181.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.181.0, P3-IA)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.181.0 / P3-IA']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3ia_v3_181_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-IA 3.181.0: {count} files written")
