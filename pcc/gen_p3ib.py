import os
MODULES = ['pcc_neuro_ext83', 'pcc_pediatric_neuro_ext72', 'pcc_pediatric_surg_ext72']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T14:30:00Z"

FUNCS = {
    'pcc_neuro_ext83': [
        ('AtaxiaTelangiectasiaExt', 'const score = Math.round((0.4+Number(input.atm||0)*0.2)*100)/100;'),
        ('FriedreichAtaxiaExt', 'const score = Math.round((0.4+Number(input.repeats||500)*0.0006)*100)/100;'),
        ('SpinocerebellarAtaxiaExt', 'const score = Math.round((0.4+Number(input.repeats||40)*0.005)*100)/100;'),
        ('MSAExt', 'const score = Math.round((0.4+Number(input.autonomic_severity||3)*0.1)*100)/100;'),
        ('CerebellarAtaxiaExt', 'const score = Math.round((0.35+Number(input.cerebellar_score||3)*0.1)*100)/100;'),
        ('SensoryAtaxiaExt', 'const score = Math.round((0.35+Number(input.sensory_loss||3)*0.1)*100)/100;'),
        ('VestibularAtaxiaExt', 'const score = Math.round((0.35+Number(input.vestibular_loss||30)*0.012)*100)/100;'),
        ('AtaxiaGeneticExt', 'const score = Math.round((0.4+Number(input.gene||0)*0.15)*100)/100;'),
        ('AtaxiaRehabExt', 'const score = Math.round((0.3+Number(input.balance_score||30)*0.02)*100)/100;'),
        ('AtaxiaMetabolicExt', 'const score = Math.round((0.35+Number(input.csf_lactate||2)*0.15)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext72': [
        ('PediatricATExt', 'const score = Math.round((0.4+Number(input.atm||0)*0.2)*100)/100;'),
        ('PediatricFAExt', 'const score = Math.round((0.4+Number(input.repeats||500)*0.0006)*100)/100;'),
        ('PediatricSCAExt', 'const score = Math.round((0.4+Number(input.repeats||40)*0.005)*100)/100;'),
        ('PediatricMSAExt', 'const score = Math.round((0.4+Number(input.autonomic_sev||2)*0.15)*100)/100;'),
        ('PediatricCerebellarAtaxiaExt', 'const score = Math.round((0.35+Number(input.cerebellar_score||2)*0.15)*100)/100;'),
        ('PediatricSensoryAtaxiaExt', 'const score = Math.round((0.35+Number(input.sensory_loss||2)*0.15)*100)/100;'),
        ('PediatricVestibularAtaxiaExt', 'const score = Math.round((0.35+Number(input.vest_score||20)*0.018)*100)/100;'),
        ('PediatricAtaxiaGeneticExt', 'const score = Math.round((0.4+Number(input.gene||0)*0.15)*100)/100;'),
        ('PediatricAtaxiaRehabExt', 'const score = Math.round((0.3+Number(input.balance||30)*0.02)*100)/100;'),
        ('PediatricAtaxiaMetabolicExt', 'const score = Math.round((0.35+Number(input.lactate||2)*0.15)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext72': [
        ('PediatricAtaxiaDBSDeepStimExt', 'const score = Math.round((0.5+Number(input.frequency||130)*0.003)*100)/100;'),
        ('PediatricAtaxiaITBSurgExt', 'const score = Math.round((0.4+Number(input.atonia_score||3)*0.08)*100)/100;'),
        ('PediatricAtaxiaGeneticTestExt', 'const score = Math.round((0.3+Number(input.gene||0)*0.15)*100)/100;'),
        ('PediatricAtaxiaStemCellExt', 'const score = Math.round((0.4+Number(input.trial_count||1)*0.15)*100)/100;'),
        ('PediatricAtaxiaGeneTherapyExt', 'const score = Math.round((0.45+Number(input.trial_count||1)*0.15)*100)/100;'),
        ('PediatricAtaxiaPhysioExt', 'const score = Math.round((0.3+Number(input.sessions||20)*0.025)*100)/100;'),
        ('PediatricAtaxiaOTExt', 'const score = Math.round((0.3+Number(input.sessions||10)*0.04)*100)/100;'),
        ('PediatricAtaxiaSpeechExt', 'const score = Math.round((0.3+Number(input.sessions||10)*0.04)*100)/100;'),
        ('PediatricAtaxiaSwallowExt', 'const score = Math.round((0.3+Number(input.sessions||5)*0.06)*100)/100;'),
        ('PediatricAtaxiaAssistiveExt', 'const score = Math.round((0.3+Number(input.devices||2)*0.1)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3ib.py -- PCC v3.182.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.182.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.182.0, P3-IB)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.182.0 / P3-IB']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3ib_v3_182_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-IB 3.182.0: {count} files written")
