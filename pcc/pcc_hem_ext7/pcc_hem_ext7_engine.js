// Auto-generated by gen_p3master.py -- PCC 3.207.0
"use strict";
const TS = '2026-07-29T14:00:00Z';
const VER = '3.207.0';

function EXT7AssessmentExt(input) {
  const score = Math.round((0.3 + (Number(input.ext7) || 0) * 0.25) * 100) / 100;
  return { version: VER, module: 'pcc_hem_ext7', function: 'EXT7AssessmentExt', input, score, ts: TS };
}

function EXT7ScoreExt(input) {
  const score = Math.round((0.3 + (Number(input.ext7) || 0) * 0.36) * 100) / 100;
  return { version: VER, module: 'pcc_hem_ext7', function: 'EXT7ScoreExt', input, score, ts: TS };
}

function EXT7StageExt(input) {
  const score = Math.round((0.3 + (Number(input.ext7) || 0) * 0.37) * 100) / 100;
  return { version: VER, module: 'pcc_hem_ext7', function: 'EXT7StageExt', input, score, ts: TS };
}

function EXT7PlanExt(input) {
  const score = Math.round((0.3 + (Number(input.ext7) || 0) * 0.33) * 100) / 100;
  return { version: VER, module: 'pcc_hem_ext7', function: 'EXT7PlanExt', input, score, ts: TS };
}

function EXT7RiskExt(input) {
  const score = Math.round((0.3 + (Number(input.ext7) || 0) * 0.34) * 100) / 100;
  return { version: VER, module: 'pcc_hem_ext7', function: 'EXT7RiskExt', input, score, ts: TS };
}

function EXT7DoseExt(input) {
  const score = Math.round((0.3 + (Number(input.ext7) || 0) * 0.35) * 100) / 100;
  return { version: VER, module: 'pcc_hem_ext7', function: 'EXT7DoseExt', input, score, ts: TS };
}

function EXT7FrequencyExt(input) {
  const score = Math.round((0.3 + (Number(input.ext7) || 0) * 0.26) * 100) / 100;
  return { version: VER, module: 'pcc_hem_ext7', function: 'EXT7FrequencyExt', input, score, ts: TS };
}

function EXT7DurationExt(input) {
  const score = Math.round((0.3 + (Number(input.ext7) || 0) * 0.22) * 100) / 100;
  return { version: VER, module: 'pcc_hem_ext7', function: 'EXT7DurationExt', input, score, ts: TS };
}

function EXT7FollowupExt(input) {
  const score = Math.round((0.3 + (Number(input.ext7) || 0) * 0.23) * 100) / 100;
  return { version: VER, module: 'pcc_hem_ext7', function: 'EXT7FollowupExt', input, score, ts: TS };
}

function EXT7OutcomeExt(input) {
  const score = Math.round((0.3 + (Number(input.ext7) || 0) * 0.19) * 100) / 100;
  return { version: VER, module: 'pcc_hem_ext7', function: 'EXT7OutcomeExt', input, score, ts: TS };
}

module.exports = {
  EXT7AssessmentExt,
  EXT7ScoreExt,
  EXT7StageExt,
  EXT7PlanExt,
  EXT7RiskExt,
  EXT7DoseExt,
  EXT7FrequencyExt,
  EXT7DurationExt,
  EXT7FollowupExt,
  EXT7OutcomeExt,
};