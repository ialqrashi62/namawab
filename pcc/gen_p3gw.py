import os
MODULES = ['pcc_neuro_ext66', 'pcc_pediatric_neuro_ext55', 'pcc_pediatric_surg_ext55']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T11:40:00Z"

FUNCS = {
    'pcc_neuro_ext66': [
        ('NeuroOncologyStagingExt', 'const score = Math.round((0.4+Number(input.grade||2)*0.1)*100)/100;'),
        ('GliomaMolecularMarkerExt', 'const score = Math.round((0.3+Number(input.idh_mutation||0)*0.3)*100)/100;'),
        ('MeningiomaGradingExt', 'const score = Math.round((0.35+Number(input.who_grade||1)*0.15)*100)/100;'),
        ('PituitaryAdenomaExt', 'const score = Math.round((0.3+Number(input.size_mm||10)*0.025)*100)/100;'),
        ('SchwannomaAssessmentExt', 'const score = Math.round((0.35+Number(input.vestibular_score||3)*0.05)*100)/100;'),
        ('BrainMetastasisExt', 'const score = Math.round((0.4+Number(input.n_metastases||1)*0.1)*100)/100;'),
        ('PrimaryCNSLymphomaExt', 'const score = Math.round((0.5+Number(input.deep_seated||0)*0.15)*100)/100;'),
        ('SpinalCordTumorExt', 'const score = Math.round((0.4+Number(input.level||3)*0.05)*100)/100;'),
        ('TumorTreatmentResponseExt', 'const score = Math.round((0.3+Number(input.rrt_score||5)*0.04)*100)/100;'),
        ('NeuroOncRehabilitationExt', 'const score = Math.round((0.35+Number(input.kps_score||70)*0.005)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext55': [
        ('PediatricBrainTumorStagingExt', 'const score = Math.round((0.4+Number(input.grade||2)*0.1)*100)/100;'),
        ('PediatricGliomaMolecularExt', 'const score = Math.round((0.3+Number(input.braf_v600e||0)*0.3)*100)/100;'),
        ('PediatricMedulloblastomaExt', 'const score = Math.round((0.45+Number(input.subtype_risk||1)*0.15)*100)/100;'),
        ('PediatricEpendymomaExt', 'const score = Math.round((0.4+Number(input.location_score||2)*0.1)*100)/100;'),
        ('PediatricATRTClassificationExt', 'const score = Math.round((0.5+Number(input.age_months||12)*0.01)*100)/100;'),
        ('PediatricDNETExt', 'const score = Math.round((0.3+Number(input.seizure_freq||5)*0.05)*100)/100;'),
        ('PediatricCraniopharyngiomaExt', 'const score = Math.round((0.4+Number(input.cyst_volume||10)*0.03)*100)/100;'),
        ('PediatricPinealtumorExt', 'const score = Math.round((0.45+Number(input.parinaud||0)*0.15)*100)/100;'),
        ('PediatricBrainstemGliomaExt', 'const score = Math.round((0.5+Number(input.dipg||0)*0.2)*100)/100;'),
        ('PediatricNeuroOncFollowupExt', 'const score = Math.round((0.3+Number(input.months_since_dx||12)*0.02)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext55': [
        ('PediatricTumorResectionExt', 'const score = Math.round((0.4+Number(input.extent_resection||50)*0.01)*100)/100;'),
        ('PediatricAwakeCraniotomyExt', 'const score = Math.round((0.5+Number(input.age_appropriate||0)*0.1)*100)/100;'),
        ('PediatricIntraoperativeMRIExt', 'const score = Math.round((0.4+Number(input.remnant_ml||2)*0.05)*100)/100;'),
        ('PediatricLaserAblationExt', 'const score = Math.round((0.4+Number(input.laser_fiber_count||1)*0.1)*100)/100;'),
        ('PediatricGliomaSurgeryExt', 'const score = Math.round((0.5+Number(input.eor_score||50)*0.01)*100)/100;'),
        ('PediatricVPShuntTumorExt', 'const score = Math.round((0.35+Number(input.hydrocephalus||0)*0.15)*100)/100;'),
        ('PediatricCranioplastyExt', 'const score = Math.round((0.3+Number(input.defect_cm||5)*0.05)*100)/100;'),
        ('PediatricSpinalTumorSurgeryExt', 'const score = Math.round((0.5+Number(input.levels_involved||2)*0.1)*100)/100;'),
        ('PediatricEndoscopicResectionExt', 'const score = Math.round((0.4+Number(input.endoscope_size||4)*0.05)*100)/100;'),
        ('PediatricBiopsySurgeryExt', 'const score = Math.round((0.3+Number(input.needle_count||3)*0.04)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3gw.py -- PCC v3.165.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.165.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.165.0, P3-GW)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.165.0 / P3-GW']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3gw_v3_165_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-GW 3.165.0: {count} files written")