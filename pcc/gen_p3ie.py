import os
MODULES = ['pcc_neuro_ext86', 'pcc_pediatric_neuro_ext75', 'pcc_pediatric_surg_ext75']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T15:00:00Z"

FUNCS = {
    'pcc_neuro_ext86': [
        ('SialorrheaManagementExt', 'const score = Math.round((0.3+Number(input.drool_severity||3)*0.1)*100)/100;'),
        ('SpasticityOralExt', 'const score = Math.round((0.3+Number(input.tone_score||3)*0.1)*100)/100;'),
        ('DysarthriaAssessmentExt', 'const score = Math.round((0.3+Number(input.intelligibility||50)*0.01)*100)/100;'),
        ('DysphagiaScreeningExt', 'const score = Math.round((0.3+Number(input.aspiration_risk||30)*0.012)*100)/100;'),
        ('PEGPlacementExt', 'const score = Math.round((0.35+Number(input.weight_loss_pct||10)*0.03)*100)/100;'),
        ('TracheostomyDecannulationExt', 'const score = Math.round((0.4+Number(input.vent_days||30)*0.005)*100)/100;'),
        ('RespiratoryAssessmentExt', 'const score = Math.round((0.3+Number(input.fev1_pct||50)*0.01)*100)/100;'),
        ('VentManagementExt', 'const score = Math.round((0.4+Number(input.vent_hours||12)*0.025)*100)/100;'),
        ('SleepApneaStrokeExt', 'const score = Math.round((0.4+Number(input.ahi||15)*0.025)*100)/100;'),
        ('GIAssessmentNeuroExt', 'const score = Math.round((0.3+Number(input.feeding_score||30)*0.02)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext75': [
        ('PediatricSialorrheaExt', 'const score = Math.round((0.3+Number(input.drool_score||3)*0.1)*100)/100;'),
        ('PediatricSpasticityOralExt', 'const score = Math.round((0.3+Number(input.tone_score||2)*0.15)*100)/100;'),
        ('PediatricDysarthriaExt', 'const score = Math.round((0.3+Number(input.intell_score||50)*0.01)*100)/100;'),
        ('PediatricDysphagiaExt', 'const score = Math.round((0.3+Number(input.aspiration_risk||20)*0.018)*100)/100;'),
        ('PediatricPEGExt', 'const score = Math.round((0.35+Number(input.weight_loss_pct||5)*0.06)*100)/100;'),
        ('PediatricTracheostomyDecannExt', 'const score = Math.round((0.4+Number(input.vent_days||20)*0.01)*100)/100;'),
        ('PediatricRespAssessmentExt', 'const score = Math.round((0.3+Number(input.fev1_pct||60)*0.01)*100)/100;'),
        ('PediatricVentMgmtExt', 'const score = Math.round((0.4+Number(input.vent_hours||10)*0.03)*100)/100;'),
        ('PediatricSleepApneaExt', 'const score = Math.round((0.4+Number(input.ahi||5)*0.04)*100)/100;'),
        ('PediatricGIAssessmentExt', 'const score = Math.round((0.3+Number(input.feeding_score||30)*0.02)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext75': [
        ('PediatricTonsillectomyOSASurgExt', 'const score = Math.round((0.4+Number(input.ahi_pre||20)*0.02)*100)/100;'),
        ('PediatricSupraglottoplastyExt', 'const score = Math.round((0.4+Number(input.laryngomalacia||2)*0.1)*100)/100;'),
        ('PediatricLaryngotrachealSurgExt', 'const score = Math.round((0.5+Number(input.subglottic||2)*0.1)*100)/100;'),
        ('PediatricBronchoscopyExt', 'const score = Math.round((0.4+Number(input.fb_count||2)*0.08)*100)/100;'),
        ('PediatricTrachealReconstructionExt', 'const score = Math.round((0.5+Number(input.stenosis_cm||1)*0.15)*100)/100;'),
        ('PediatricPEGInsertionExt', 'const score = Math.round((0.35+Number(input.weight_loss_pct||5)*0.06)*100)/100;'),
        ('PediatricGJTubesExt', 'const score = Math.round((0.35+Number(input.feeding_score||3)*0.1)*100)/100;'),
        ('PediatricFundoplicationExt', 'const score = Math.round((0.35+Number(input.reflux_score||3)*0.1)*100)/100;'),
        ('PediatricTracheostomyPlastyExt', 'const score = Math.round((0.5+Number(input.granulation||1)*0.15)*100)/100;'),
        ('PediatricChestWallReconstructionExt', 'const score = Math.round((0.4+Number(input.defect||3)*0.1)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3ie.py -- PCC v3.185.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.185.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.185.0, P3-IE)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.185.0 / P3-IE']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3ie_v3_185_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-IE 3.185.0: {count} files written")
