import os
MODULES = ['pcc_neuro_ext64', 'pcc_pediatric_neuro_ext53', 'pcc_pediatric_surg_ext53']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T11:20:00Z"

FUNCS = {
    'pcc_neuro_ext64': [
        ('IdiopathicIntracranialHypertensionExt3', 'const score = Math.round((0.3+Number(input.opening_pressure||25)/50)*100)/100;'),
        ('VenousSinusStenosisStentingExt', 'const score = Math.round((0.4+Number(input.gradient_mmHg||8)/30)*100)/100;'),
        ('CSFLeakSiteLocalizationExt', 'const score = Math.round((0.2+Number(input.beta2transferrin||0)*0.6)*100)/100;'),
        ('SpontaneousIntracranialHypotensionExt', 'const score = Math.round((0.3+Number(input.ortho_headache||1)*0.05)*100)/100;'),
        ('ChiariMalformationComplexExt', 'const score = Math.round((0.4+Number(input.tonsil_mm||6)*0.04)*100)/100;'),
        ('SyringomyeliaMonitoringExt', 'const score = Math.round((0.3+Number(input.cyst_length_mm||10)*0.02)*100)/100;'),
        ('BasilarInvaginationExt', 'const score = Math.round((0.45+Number(input.basion_mm||5)*0.04)*100)/100;'),
        ('CraniovertebralJunctionAnomalyExt', 'const score = Math.round((0.4+Number(input.cvj_angle_deg||140)*0.003)*100)/100;'),
        ('CSFFlowDynamicsExt', 'const score = Math.round((0.35+Number(input.flow_velocity||10)*0.04)*100)/100;'),
        ('EmptySellaSyndromeMonitorExt', 'const score = Math.round((0.3+Number(input.pituitary_height||2)*0.15)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext53': [
        ('PediatricIIHFollowupExt', 'const score = Math.round((0.3+Number(input.opening_pressure||20)/50)*100)/100;'),
        ('PediatricVenousSinusStentExt', 'const score = Math.round((0.4+Number(input.gradient_mmHg||6)/30)*100)/100;'),
        ('PediatricCSFLeakMonitorExt', 'const score = Math.round((0.2+Number(input.beta2transferrin||0)*0.6)*100)/100;'),
        ('PediatricIntracranialHypotensionExt', 'const score = Math.round((0.3+Number(input.ortho_headache||0)*0.05)*100)/100;'),
        ('PediatricChiariAssessmentExt', 'const score = Math.round((0.4+Number(input.tonsil_mm||5)*0.04)*100)/100;'),
        ('PediatricSyringomyeliaFollowExt', 'const score = Math.round((0.3+Number(input.cyst_length_mm||8)*0.02)*100)/100;'),
        ('PediatricBasilarInvaginationExt', 'const score = Math.round((0.45+Number(input.basion_mm||4)*0.04)*100)/100;'),
        ('PediatricCVJAnomalyExt', 'const score = Math.round((0.4+Number(input.cvj_angle_deg||140)*0.003)*100)/100;'),
        ('PediatricCSFFlowDynamicsExt', 'const score = Math.round((0.35+Number(input.flow_velocity||8)*0.04)*100)/100;'),
        ('PediatricEmptySellaMonitorExt', 'const score = Math.round((0.3+Number(input.pituitary_height||2)*0.15)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext53': [
        ('PediatricVenousSinusStentSurgeryExt', 'const score = Math.round((0.4+Number(input.stent_diameter_mm||6)/20)*100)/100;'),
        ('PediatricVPShuntTapTestExt', 'const score = Math.round((0.2+Number(input.tap_response||1)*0.08)*100)/100;'),
        ('PediatricChiariDecompressionExt', 'const score = Math.round((0.4+Number(input.bone_removal_mm||10)*0.03)*100)/100;'),
        ('PediatricSyrinxShuntExt', 'const score = Math.round((0.35+Number(input.syrinx_size_mm||8)*0.04)*100)/100;'),
        ('PediatricBasilarInvaginationSurgeryExt', 'const score = Math.round((0.5+Number(input.reduction_mm||5)*0.05)*100)/100;'),
        ('PediatricOccipitalCervicalFusionExt', 'const score = Math.round((0.45+Number(input.levels_fused||3)*0.05)*100)/100;'),
        ('PediatricVPShuntPlacementExt', 'const score = Math.round((0.3+Number(input.catheter_length||30)*0.01)*100)/100;'),
        ('PediatricEndoscopicThirdVentriculostomyExt', 'const score = Math.round((0.4+Number(input.ostomy_size||5)*0.05)*100)/100;'),
        ('PediatricCSFDiversionExt', 'const score = Math.round((0.35+Number(input.drain_Level||1)*0.04)*100)/100;'),
        ('PediatricEndoscopicFenestrationExt', 'const score = Math.round((0.4+Number(input.fenestrations||2)*0.06)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3gu.py -- PCC v3.163.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.163.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines).encode('utf-8').decode('utf-8'))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines).encode('utf-8').decode('utf-8'))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines).encode('utf-8').decode('utf-8'))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines).encode('utf-8').decode('utf-8'))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.163.0, P3-GU)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines).encode('utf-8').decode('utf-8'))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.163.0 / P3-GU']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3gu_v3_163_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines).encode('utf-8').decode('utf-8'))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-GU 3.163.0: {count} files written")