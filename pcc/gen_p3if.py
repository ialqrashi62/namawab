import os
MODULES = ['pcc_neuro_ext87', 'pcc_pediatric_neuro_ext76', 'pcc_pediatric_surg_ext76']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T15:10:00Z"

FUNCS = {
    'pcc_neuro_ext87': [
        ('CognitiveDisorderExt', 'const score = Math.round((0.3+Number(input.moca_score||22)*0.03)*100)/100;'),
        ('AnosognosiaExt', 'const score = Math.round((0.4+Number(input.awareness_score||2)*0.15)*100)/100;'),
        ('ApraxiaExt', 'const score = Math.round((0.35+Number(input.apraxia_score||3)*0.1)*100)/100;'),
        ('AgnosiaExt', 'const score = Math.round((0.35+Number(input.agnosia_score||3)*0.1)*100)/100;'),
        ('ExecutiveDysfunctionExt', 'const score = Math.round((0.35+Number(input.tmt_b_seconds||90)*0.005)*100)/100;'),
        ('MemoryDisorderExt', 'const score = Math.round((0.3+Number(input.memory_score||40)*0.012)*100)/100;'),
        ('VisuospatialExt', 'const score = Math.round((0.35+Number(input.vsp_score||30)*0.015)*100)/100;'),
        ('LanguageDisorderExt', 'const score = Math.round((0.35+Number(input.naming_score||40)*0.012)*100)/100;'),
        ('BehavioralDysexecutiveExt', 'const score = Math.round((0.4+Number(input.disinhibition||2)*0.15)*100)/100;'),
        ('SocialCognitionExt', 'const score = Math.round((0.4+Number(input.social_score||3)*0.08)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext76': [
        ('PediatricCognitiveDisorderExt', 'const score = Math.round((0.3+Number(input.cog_score||80)*0.005)*100)/100;'),
        ('PediatricAnosognosiaExt', 'const score = Math.round((0.4+Number(input.awareness||2)*0.15)*100)/100;'),
        ('PediatricApraxiaExt', 'const score = Math.round((0.35+Number(input.apraxia_score||2)*0.15)*100)/100;'),
        ('PediatricAgnosiaExt', 'const score = Math.round((0.35+Number(input.agnosia_score||2)*0.15)*100)/100;'),
        ('PediatricExecDysfunctionExt', 'const score = Math.round((0.35+Number(input.tmt_b||60)*0.008)*100)/100;'),
        ('PediatricMemoryExt', 'const score = Math.round((0.3+Number(input.memory||40)*0.012)*100)/100;'),
        ('PediatricVisuospatialExt', 'const score = Math.round((0.35+Number(input.vsp||30)*0.015)*100)/100;'),
        ('PediatricLanguageDisorderExt', 'const score = Math.round((0.35+Number(input.naming||40)*0.012)*100)/100;'),
        ('PediatricBehavioralExecExt', 'const score = Math.round((0.4+Number(input.disinhibition||2)*0.15)*100)/100;'),
        ('PediatricTheoryMindExt', 'const score = Math.round((0.4+Number(input.screening_score||3)*0.08)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext76': [
        ('PediatricWadaTestExt', 'const score = Math.round((0.4+Number(input.right_score||50)*0.01)*100)/100;'),
        ('PediatricMEGSourceImagingExt', 'const score = Math.round((0.4+Number(input.sources||50)*0.01)*100)/100;'),
        ('PediatricFunctionalMRSurgeryExt', 'const score = Math.round((0.4+Number(input.tumor_score||50)*0.01)*100)/100;'),
        ('PediatricLanguageMappingSurgExt', 'const score = Math.round((0.4+Number(input.broca||1)*0.15)*100)/100;'),
        ('PediatricMemoryMappingExt', 'const score = Math.round((0.4+Number(input.hippocampal||1)*0.15)*100)/100;'),
        ('PediatricAwakeCraniotomyExt', 'const score = Math.round((0.5+Number(input.age||16)*0.01)*100)/100;'),
        ('PediatricEpilepsySurgeryEvalExt', 'const score = Math.round((0.4+Number(input.focal_score||1)*0.15)*100)/100;'),
        ('PediatricSurgicalResectionCognExt', 'const score = Math.round((0.4+Number(input.preserve_score||1)*0.15)*100)/100;'),
        ('PediatricCogRehabPostExt', 'const score = Math.round((0.3+Number(input.weeks||8)*0.05)*100)/100;'),
        ('PediatricCognitiveScreeningExt', 'const score = Math.round((0.3+Number(input.tests||5)*0.06)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3if.py -- PCC v3.186.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.186.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.186.0, P3-IF)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.186.0 / P3-IF']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3if_v3_186_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-IF 3.186.0: {count} files written")
