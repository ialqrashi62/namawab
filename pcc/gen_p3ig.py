import os
MODULES = ['pcc_neuro_ext88', 'pcc_pediatric_neuro_ext77', 'pcc_pediatric_surg_ext77']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T15:20:00Z"

FUNCS = {
    'pcc_neuro_ext88': [
        ('MedicalRefractoryEpilepsyExt', 'const score = Math.round((0.45+Number(input.failed_drugs||3)*0.1)*100)/100;'),
        ('SurgicalEpilepsyEvalExt', 'const score = Math.round((0.4+Number(input.localization||1)*0.15)*100)/100;'),
        ('LaserAblationExt', 'const score = Math.round((0.4+Number(input.depth_mm||10)*0.025)*100)/100;'),
        ('RNSExt', 'const score = Math.round((0.4+Number(input.detections||5)*0.04)*100)/100;'),
        ('DBSForEpilepsyExt', 'const score = Math.round((0.45+Number(input.seizure_freq||5)*0.05)*100)/100;'),
        ('VagusNerveTuneExt', 'const score = Math.round((0.3+Number(input.output||1)*0.05)*100)/100;'),
        ('KetogenicDietNeuroExt', 'const score = Math.round((0.3+Number(input.ketone||3)*0.1)*100)/100;'),
        ('ACTHExt', 'const score = Math.round((0.3+Number(input.dose_mg||80)*0.005)*100)/100;'),
        ('EpilepsyGeneticExt', 'const score = Math.round((0.4+Number(input.variant||1)*0.15)*100)/100;'),
        ('SuddenUnexpectedDeathEpilepsyRiskExt', 'const score = Math.round((0.4+Number(input.seizure_type||2)*0.1)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext77': [
        ('PediatricMedicalRefractoryExt', 'const score = Math.round((0.45+Number(input.failed_drugs||2)*0.1)*100)/100;'),
        ('PediatricSurgicalEpilepsyExt', 'const score = Math.round((0.4+Number(input.localization||1)*0.15)*100)/100;'),
        ('PediatricLaserAblationExt', 'const score = Math.round((0.4+Number(input.depth_mm||8)*0.03)*100)/100;'),
        ('PediatricRNSExt', 'const score = Math.round((0.4+Number(input.detections||3)*0.06)*100)/100;'),
        ('PediatricDBSForEpilepsyExt', 'const score = Math.round((0.45+Number(input.seizure_freq||3)*0.06)*100)/100;'),
        ('PediatricVNSTuneExt', 'const score = Math.round((0.3+Number(input.output||0.5)*0.1)*100)/100;'),
        ('PediatricKetogenicExt', 'const score = Math.round((0.3+Number(input.ketone||3)*0.1)*100)/100;'),
        ('PediatricACTHExt', 'const score = Math.round((0.3+Number(input.dose_mg||40)*0.01)*100)/100;'),
        ('PediatricEpilepsyGeneticExt', 'const score = Math.round((0.4+Number(input.variant||1)*0.15)*100)/100;'),
        ('PediatricSUDEPExt', 'const score = Math.round((0.4+Number(input.seizure_type||2)*0.1)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext77': [
        ('PediatricLaserAblationSurgExt', 'const score = Math.round((0.4+Number(input.depth||8)*0.03)*100)/100;'),
        ('PediatricSEEGPlacementExt', 'const score = Math.round((0.4+Number(input.electrodes||8)*0.04)*100)/100;'),
        ('PediatricStripGridPlacementExt', 'const score = Math.round((0.4+Number(input.contacts||64)*0.005)*100)/100;'),
        ('PediatricPhase2MonitoringExt', 'const score = Math.round((0.3+Number(input.days||5)*0.05)*100)/100;'),
        ('PediatricResectiveSurgeryExt', 'const score = Math.round((0.45+Number(input.eor||50)*0.01)*100)/100;'),
        ('PediatricAHSSSurgeryExt', 'const score = Math.round((0.5+Number(input.daily_seizures||3)*0.1)*100)/100;'),
        ('PediatricCorpusCallosotomySurgExt', 'const score = Math.round((0.5+Number(input.drop_seizures||2)*0.1)*100)/100;'),
        ('PediatricLesionectomySurgExt', 'const score = Math.round((0.4+Number(input.lesion_size||2)*0.1)*100)/100;'),
        ('PediatricMinimallyInvasiveExt', 'const score = Math.round((0.4+Number(input.min_inv_approach||1)*0.15)*100)/100;'),
        ('PediatricEpilepsyRehabPostExt', 'const score = Math.round((0.3+Number(input.weeks||8)*0.05)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3ig.py -- PCC v3.187.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.187.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.187.0, P3-IG)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.187.0 / P3-IG']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3ig_v3_187_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-IG 3.187.0: {count} files written")
