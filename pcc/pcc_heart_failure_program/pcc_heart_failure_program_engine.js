// Auto-generated by gen_p3master.py -- PCC 3.188.0
"use strict";
const TS = '2026-07-29T14:00:00Z';
const VER = '3.188.0';

function HFPAssessmentExt(input) {
  const score = Math.round((0.3 + (Number(input.hfpa) || 0) * 0.2) * 100) / 100;
  return { version: VER, module: 'pcc_heart_failure_program', function: 'HFPAssessmentExt', input, score, ts: TS };
}

function HFPScoreExt(input) {
  const score = Math.round((0.3 + (Number(input.hfps) || 0) * 0.31) * 100) / 100;
  return { version: VER, module: 'pcc_heart_failure_program', function: 'HFPScoreExt', input, score, ts: TS };
}

function HFPStageExt(input) {
  const score = Math.round((0.3 + (Number(input.hfps) || 0) * 0.32) * 100) / 100;
  return { version: VER, module: 'pcc_heart_failure_program', function: 'HFPStageExt', input, score, ts: TS };
}

function HFPPlanExt(input) {
  const score = Math.round((0.3 + (Number(input.hfpp) || 0) * 0.28) * 100) / 100;
  return { version: VER, module: 'pcc_heart_failure_program', function: 'HFPPlanExt', input, score, ts: TS };
}

function HFPRiskExt(input) {
  const score = Math.round((0.3 + (Number(input.hfpr) || 0) * 0.29) * 100) / 100;
  return { version: VER, module: 'pcc_heart_failure_program', function: 'HFPRiskExt', input, score, ts: TS };
}

function HFPDoseExt(input) {
  const score = Math.round((0.3 + (Number(input.hfpd) || 0) * 0.3) * 100) / 100;
  return { version: VER, module: 'pcc_heart_failure_program', function: 'HFPDoseExt', input, score, ts: TS };
}

function HFPFrequencyExt(input) {
  const score = Math.round((0.3 + (Number(input.hfpf) || 0) * 0.21) * 100) / 100;
  return { version: VER, module: 'pcc_heart_failure_program', function: 'HFPFrequencyExt', input, score, ts: TS };
}

function HFPDurationExt(input) {
  const score = Math.round((0.3 + (Number(input.hfpd) || 0) * 0.17) * 100) / 100;
  return { version: VER, module: 'pcc_heart_failure_program', function: 'HFPDurationExt', input, score, ts: TS };
}

function HFPFollowupExt(input) {
  const score = Math.round((0.3 + (Number(input.hfpf) || 0) * 0.18) * 100) / 100;
  return { version: VER, module: 'pcc_heart_failure_program', function: 'HFPFollowupExt', input, score, ts: TS };
}

function HFPOutcomeExt(input) {
  const score = Math.round((0.3 + (Number(input.hfpo) || 0) * 0.49) * 100) / 100;
  return { version: VER, module: 'pcc_heart_failure_program', function: 'HFPOutcomeExt', input, score, ts: TS };
}

module.exports = {
  HFPAssessmentExt,
  HFPScoreExt,
  HFPStageExt,
  HFPPlanExt,
  HFPRiskExt,
  HFPDoseExt,
  HFPFrequencyExt,
  HFPDurationExt,
  HFPFollowupExt,
  HFPOutcomeExt,
};