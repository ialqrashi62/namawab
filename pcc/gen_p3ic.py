import os
MODULES = ['pcc_neuro_ext84', 'pcc_pediatric_neuro_ext73', 'pcc_pediatric_surg_ext73']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T14:40:00Z"

FUNCS = {
    'pcc_neuro_ext84': [
        ('DementiaScreeningExt', 'const score = Math.round((0.3+Number(input.mmse||25)*0.025)*100)/100;'),
        ('AlzheimersDementiaExt', 'const score = Math.round((0.4+Number(input.atrophy||2)*0.1)*100)/100;'),
        ('LewyBodyDementiaExt', 'const score = Math.round((0.4+Number(input.visual_hallucination||0)*0.2)*100)/100;'),
        ('VascularDementiaExt', 'const score = Math.round((0.4+Number(input.fazekas||1)*0.15)*100)/100;'),
        ('FTDBehavioralExt', 'const score = Math.round((0.4+Number(input.disinhibition||2)*0.15)*100)/100;'),
        ('FTDLanguageExt', 'const score = Math.round((0.4+Number(input.language_score||2)*0.15)*100)/100;'),
        ('PosteriorCorticalAtrophyExt', 'const score = Math.round((0.4+Number(input.visual_field||2)*0.15)*100)/100;'),
        ('DLBvsADDExt', 'const score = Math.round((0.4+Number(input.caregiver_ih||0)*0.2)*100)/100;'),
        ('DementiaTreatmentExt', 'const score = Math.round((0.3+Number(input.drug_count||1)*0.15)*100)/100;'),
        ('DementiaBehavioralExt', 'const score = Math.round((0.3+Number(input.behavioral_score||2)*0.15)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext73': [
        ('PediatricDementiaScreeningExt', 'const score = Math.round((0.3+Number(input.cog_score||80)*0.005)*100)/100;'),
        ('PediatricNiemannPickExt', 'const score = Math.round((0.4+Number(input.splenomegaly||0)*0.2)*100)/100;'),
        ('PediatricTaySachsExt', 'const score = Math.round((0.4+Number(input.hex_a||0)*0.2)*100)/100;'),
        ('PediatricBattenDiseaseExt', 'const score = Math.round((0.4+Number(input.cln3||0)*0.2)*100)/100;'),
        ('PediatricLeukodystrophyExt', 'const score = Math.round((0.4+Number(input.mri_charcot||1)*0.15)*100)/100;'),
        ('PediatricALDGenExt', 'const score = Math.round((0.4+Number(input.abc||0)*0.2)*100)/100;'),
        ('PediatricPKUExt', 'const score = Math.round((0.4+Number(input.phe_level||2)*0.2)*100)/100;'),
        ('PediatricMLDExt', 'const score = Math.round((0.4+Number(input.substrate||0)*0.2)*100)/100;'),
        ('PediatricMitochondrialExt', 'const score = Math.round((0.4+Number(input.mtdna||0)*0.15)*100)/100;'),
        ('PediatricScreenDevelopmentalExt', 'const score = Math.round((0.3+Number(input.developmental_age||0)*0.05)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext73': [
        ('PediatricKetogenicDietExt', 'const score = Math.round((0.4+Number(input.ketone||3)*0.1)*100)/100;'),
        ('PediatricVagusNerveSurgExt', 'const score = Math.round((0.35+Number(input.output||1)*0.1)*100)/100;'),
        ('PediatricCallosotomyExt', 'const score = Math.round((0.5+Number(input.drop_seizures||2)*0.1)*100)/100;'),
        ('PediatricHemispherectomyExt', 'const score = Math.round((0.5+Number(input.daily_seizures||3)*0.1)*100)/100;'),
        ('PediatricLesionectomyExt', 'const score = Math.round((0.4+Number(input.lesion_size||2)*0.1)*100)/100;'),
        ('PediatricLaserAblationExt', 'const score = Math.round((0.4+Number(input.laser_fibers||1)*0.1)*100)/100;'),
        ('PediatricRNSSurgExt', 'const score = Math.round((0.4+Number(input.detections||10)*0.02)*100)/100;'),
        ('PediatricCordotomyExt', 'const score = Math.round((0.5+Number(input.pain_score||7)*0.05)*100)/100;'),
        ('PediatricITBSurgExt', 'const score = Math.round((0.4+Number(input.dose_mg||30)*0.012)*100)/100;'),
        ('PediatricNeurostimExt', 'const score = Math.round((0.35+Number(input.elegibility||1)*0.15)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3ic.py -- PCC v3.183.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.183.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.183.0, P3-IC)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.183.0 / P3-IC']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3ic_v3_183_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-IC 3.183.0: {count} files written")
