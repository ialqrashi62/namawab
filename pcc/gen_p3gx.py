import os
MODULES = ['pcc_neuro_ext67', 'pcc_pediatric_neuro_ext56', 'pcc_pediatric_surg_ext56']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T11:50:00Z"

FUNCS = {
    'pcc_neuro_ext67': [
        ('StrokeRecoveryAssessmentExt', 'const score = Math.round((0.3+Number(input.days_post||30)*0.005)*100)/100;'),
        ('AphasiaAssessmentExt', 'const score = Math.round((0.4+Number(input.wab_score||50)*0.005)*100)/100;'),
        ('DysphagiaManagementExt', 'const score = Math.round((0.35+Number(input.gugging_score||10)*0.04)*100)/100;'),
        ('SpasticityTreatmentExt', 'const score = Math.round((0.4+Number(input.mas_score||2)*0.1)*100)/100;'),
        ('NeurogenicBladderExt', 'const score = Math.round((0.3+Number(input.residual_ml||100)*0.003)*100)/100;'),
        ('PoststrokeDepressionExt', 'const score = Math.round((0.4+Number(input.phq9_score||10)*0.04)*100)/100;'),
        ('PoststrokeSeizureExt', 'const score = Math.round((0.4+Number(input.seizure_freq||1)*0.15)*100)/100;'),
        ('MotorRecoveryTrackingExt', 'const score = Math.round((0.3+Number(input.fugl_meyer||50)*0.008)*100)/100;'),
        ('CognitiveRehabExt', 'const score = Math.round((0.35+Number(input.mmse_score||24)*0.02)*100)/100;'),
        ('VocationalRehabExt', 'const score = Math.round((0.3+Number(input.work_hours||4)*0.05)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext56': [
        ('PediatricStrokeRecoveryExt', 'const score = Math.round((0.3+Number(input.days_post||30)*0.005)*100)/100;'),
        ('PediatricAphasiaExt', 'const score = Math.round((0.4+Number(input.caps_score||30)*0.01)*100)/100;'),
        ('PediatricDysphagiaExt', 'const score = Math.round((0.35+Number(input.feeding_score||5)*0.05)*100)/100;'),
        ('PediatricSpasticityExt', 'const score = Math.round((0.4+Number(input.mas_score||2)*0.1)*100)/100;'),
        ('PediatricNeurogenicBladderExt', 'const score = Math.round((0.3+Number(input.residual_ml||50)*0.005)*100)/100;'),
        ('PediatricPoststrokeDepressionExt', 'const score = Math.round((0.4+Number(input.cd_score||5)*0.06)*100)/100;'),
        ('PediatricPoststrokeSeizureExt', 'const score = Math.round((0.4+Number(input.seizure_freq||1)*0.15)*100)/100;'),
        ('PediatricMotorRecoveryExt', 'const score = Math.round((0.3+Number(input.gms_score||50)*0.008)*100)/100;'),
        ('PediatricCogRehabExt', 'const score = Math.round((0.35+Number(input.brief_score||50)*0.01)*100)/100;'),
        ('PediatricSchoolReintegrationExt', 'const score = Math.round((0.3+Number(input.school_hours||3)*0.05)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext56': [
        ('PediatricSelectiveDorsalRhizotomyExt', 'const score = Math.round((0.4+Number(input.rootlets||5)*0.05)*100)/100;'),
        ('PediatricIntrathecalBaclofenSurgeryExt', 'const score = Math.round((0.4+Number(input.catheter_level||2)*0.08)*100)/100;'),
        ('PediatricTendonLengtheningExt', 'const score = Math.round((0.3+Number(input.tendons||3)*0.05)*100)/100;'),
        ('PediatricBotulinumInjectionSurgeryExt', 'const score = Math.round((0.3+Number(input.muscles||4)*0.05)*100)/100;'),
        ('PediatricOrthopedicSpineSurgeryExt', 'const score = Math.round((0.5+Number(input.cobb_angle||40)*0.01)*100)/100;'),
        ('PediatricHipReconstructionExt', 'const score = Math.round((0.45+Number(input.reimers||30)*0.01)*100)/100;'),
        ('PediatricGaitSurgeryExt', 'const score = Math.round((0.35+Number(input.levels||3)*0.05)*100)/100;'),
        ('PediatricUpperLimbSurgeryExt', 'const score = Math.round((0.3+Number(input.muscles||4)*0.05)*100)/100;'),
        ('PediatricSpasticitySurgeryExt', 'const score = Math.round((0.4+Number(input.spasm_score||2)*0.1)*100)/100;'),
        ('PediatricCerebralPalsySurgeryExt', 'const score = Math.round((0.4+Number(input.gmfcs||2)*0.1)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3gx.py -- PCC v3.166.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.166.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.166.0, P3-GX)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.166.0 / P3-GX']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3gx_v3_166_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-GX 3.166.0: {count} files written")