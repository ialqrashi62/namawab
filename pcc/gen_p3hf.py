import os
MODULES = ['pcc_neuro_ext75', 'pcc_pediatric_neuro_ext64', 'pcc_pediatric_surg_ext64']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T13:10:00Z"

FUNCS = {
    'pcc_neuro_ext75': [
        ('MyastheniaGravisExt', 'const score = Math.round((0.3+Number(input.adl_score||5)*0.07)*100)/100;'),
        ('LambertEatonExt', 'const score = Math.round((0.4+Number(input.cmamp||2)*0.1)*100)/100;'),
        ('MyasthenicCrisisExt', 'const score = Math.round((0.5+Number(input.vital||20)*0.015)*100)/100;'),
        ('CholinergicCrisisExt', 'const score = Math.round((0.5+Number(input.symptoms||3)*0.08)*100)/100;'),
        ('OcularMyastheniaExt', 'const score = Math.round((0.3+Number(input.ocular_score||3)*0.08)*100)/100;'),
        ('ThymomaAssociatedExt', 'const score = Math.round((0.4+Number(input.thymus_size||3)*0.08)*100)/100;'),
        ('MUSKAntibodyMGExt', 'const score = Math.round((0.4+Number(input.musk_titer||0)*0.001)*100)/100;'),
        ('LRP4MGExt', 'const score = Math.round((0.4+Number(input.lrp4_titer||0)*0.001)*100)/100;'),
        ('SeronegativeMGExt', 'const score = Math.round((0.4+Number(input.achr_pct||0)*0.01)*100)/100;'),
        ('MGQOL15Ext', 'const score = Math.round((0.3+Number(input.qol_score||15)*0.04)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext64': [
        ('PediatricMyastheniaGravisExt', 'const score = Math.round((0.3+Number(input.adl_score||4)*0.07)*100)/100;'),
        ('PediatricLambertEatonExt', 'const score = Math.round((0.4+Number(input.cmamp||1)*0.15)*100)/100;'),
        ('PediatricMyasthenicCrisisExt', 'const score = Math.round((0.5+Number(input.vital||20)*0.015)*100)/100;'),
        ('PediatricCholinergicCrisisExt', 'const score = Math.round((0.5+Number(input.symptoms||2)*0.1)*100)/100;'),
        ('PediatricOcularMGExt', 'const score = Math.round((0.3+Number(input.ocular_score||2)*0.1)*100)/100;'),
        ('PediatricThymomaExt', 'const score = Math.round((0.4+Number(input.thymus_size||2)*0.1)*100)/100;'),
        ('PediatricMUSKAntibodyExt', 'const score = Math.round((0.4+Number(input.musk_titer||0)*0.001)*100)/100;'),
        ('PediatricLRP4Ext', 'const score = Math.round((0.4+Number(input.lrp4_titer||0)*0.001)*100)/100;'),
        ('PediatricSeronegativeMGExt', 'const score = Math.round((0.4+Number(input.achr_pct||0)*0.01)*100)/100;'),
        ('PediatricMGQOLExt', 'const score = Math.round((0.3+Number(input.qol_score||10)*0.05)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext64': [
        ('PediatricThymectomyExt', 'const score = Math.round((0.4+Number(input.thymus_size||3)*0.08)*100)/100;'),
        ('PediatricPlasmapheresisExt', 'const score = Math.round((0.35+Number(input.exchanges||5)*0.06)*100)/100;'),
        ('PediatricIVIGExt', 'const score = Math.round((0.3+Number(input.dose_gkg||1)*0.15)*100)/100;'),
        ('PediatricImmunoablativeTherapyExt', 'const score = Math.round((0.45+Number(input.cycle_count||2)*0.1)*100)/100;'),
        ('PediatricRituximabNMJExt', 'const score = Math.round((0.4+Number(input.dose_mg||375)*0.0006)*100)/100;'),
        ('PediatricECulizumabExt', 'const score = Math.round((0.5+Number(input.dose_mg||600)*0.0005)*100)/100;'),
        ('PediatricFcRNTreatmentExt', 'const score = Math.round((0.5+Number(input.dose_mg||340)*0.0008)*100)/100;'),
        ('PediatricPlasmapheresisCathExt', 'const score = Math.round((0.35+Number(input.catheter||1)*0.1)*100)/100;'),
        ('PediatricNMJDietExt', 'const score = Math.round((0.3+Number(input.calories||1500)*0.0002)*100)/100;'),
        ('PediatricSwallowingAssessExt', 'const score = Math.round((0.3+Number(input.aspiration_score||2)*0.1)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3hf.py -- PCC v3.174.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.174.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.174.0, P3-HF)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.174.0 / P3-HF']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3hf_v3_174_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-HF 3.174.0: {count} files written")
