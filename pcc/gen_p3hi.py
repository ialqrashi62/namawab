import os
MODULES = ['pcc_neuro_ext78', 'pcc_pediatric_neuro_ext67', 'pcc_pediatric_surg_ext67']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T13:40:00Z"

FUNCS = {
    'pcc_neuro_ext78': [
        ('TBIRehabExt', 'const score = Math.round((0.3+Number(input.disability_score||3)*0.08)*100)/100;'),
        ('SpinalCordInjuryRehabExt', 'const score = Math.round((0.4+Number(input.ais_level||2)*0.1)*100)/100;'),
        ('StrokeRehabExt', 'const score = Math.round((0.35+Number(input.mod_rankin||3)*0.08)*100)/100;'),
        ('BotulinumToxinExt', 'const score = Math.round((0.3+Number(input.muscles||4)*0.06)*100)/100;'),
        ('FESDeviceProgramExt', 'const score = Math.round((0.3+Number(input.channels||4)*0.05)*100)/100;'),
        ('PressureUlcerManagementExt', 'const score = Math.round((0.4+Number(input.braden_score||12)*0.025)*100)/100;'),
        ('NeurogenicBladderMgmtExt', 'const score = Math.round((0.35+Number(input.catheter_type||1)*0.15)*100)/100;'),
        ('WheelchairSeatingExt', 'const score = Math.round((0.3+Number(input.chair_score||3)*0.08)*100)/100;'),
        ('NeuroAssistiveTechExt', 'const score = Math.round((0.3+Number(input.devices||3)*0.08)*100)/100;'),
        ('OutpatientNeuroRehabExt', 'const score = Math.round((0.3+Number(input.sessions||10)*0.025)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext67': [
        ('PediatricTBIRehabExt', 'const score = Math.round((0.3+Number(input.disability_score||2)*0.1)*100)/100;'),
        ('PediatricSCIRecoveryExt', 'const score = Math.round((0.4+Number(input.ais_level||1)*0.15)*100)/100;'),
        ('PediatricStrokeRehabExt', 'const score = Math.round((0.35+Number(input.pediatric_outcome||3)*0.08)*100)/100;'),
        ('PediatricBotoxExt', 'const score = Math.round((0.3+Number(input.muscles||3)*0.07)*100)/100;'),
        ('PediatricFESExt', 'const score = Math.round((0.3+Number(input.channels||2)*0.08)*100)/100;'),
        ('PediatricPressureUlcerExt', 'const score = Math.round((0.4+Number(input.braden_score||16)*0.02)*100)/100;'),
        ('PediatricNeuroBladderMgmtExt', 'const score = Math.round((0.35+Number(input.catheter_type||1)*0.15)*100)/100;'),
        ('PediatricWheelchairExt', 'const score = Math.round((0.3+Number(input.chair_score||3)*0.08)*100)/100;'),
        ('PediatricNeuroAssistExt', 'const score = Math.round((0.3+Number(input.devices||2)*0.1)*100)/100;'),
        ('PediatricOPRehabExt', 'const score = Math.round((0.3+Number(input.sessions||8)*0.03)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext67': [
        ('PediatricToneMgmtExt', 'const score = Math.round((0.4+Number(input.spasticity_score||3)*0.08)*100)/100;'),
        ('PediatricIntrathecalPumpExt', 'const score = Math.round((0.4+Number(input.dose_mg||5)*0.05)*100)/100;'),
        ('PediatricSDRExt', 'const score = Math.round((0.45+Number(input.rootlets||5)*0.05)*100)/100;'),
        ('PediatricBotulinumSurgExt', 'const score = Math.round((0.3+Number(input.muscles||4)*0.06)*100)/100;'),
        ('PediatricConstraintTherapyExt', 'const score = Math.round((0.3+Number(input.weeks||8)*0.05)*100)/100;'),
        ('PediatricGaitTrainingExt', 'const score = Math.round((0.3+Number(input.sessions||20)*0.025)*100)/100;'),
        ('PediatricPROExt', 'const score = Math.round((0.3+Number(input.devices||1)*0.15)*100)/100;'),
        ('PediatricOrthoticCastingExt', 'const score = Math.round((0.3+Number(input.casts||3)*0.08)*100)/100;'),
        ('PediatricOrthoScoliosisMgmtExt', 'const score = Math.round((0.4+Number(input.cobb||30)*0.01)*100)/100;'),
        ('PediatricOrthosisGaitExt', 'const score = Math.round((0.3+Number(input.devices||2)*0.1)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3hi.py -- PCC v3.177.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.177.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.177.0, P3-HI)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.177.0 / P3-HI']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3hi_v3_177_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-HI 3.177.0: {count} files written")
