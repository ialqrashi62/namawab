import os
MODULES = ['pcc_neuro_ext74', 'pcc_pediatric_neuro_ext63', 'pcc_pediatric_surg_ext63']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T13:00:00Z"

FUNCS = {
    'pcc_neuro_ext74': [
        ('PeripheralNeuropathyExt', 'const score = Math.round((0.3+Number(input.ncs_amp||3)*0.06)*100)/100;'),
        ('CIDPExt', 'const score = Math.round((0.4+Number(input.efns_score||4)*0.06)*100)/100;'),
        ('GuillainBarreExt', 'const score = Math.round((0.45+Number(input.egs_score||3)*0.08)*100)/100;'),
        ('CIDPVariantExt', 'const score = Math.round((0.4+Number(input.mfs||0)*0.15)*100)/100;'),
        ('DiabeticNeuropathyExt', 'const score = Math.round((0.3+Number(input.duration||10)*0.04)*100)/100;'),
        ('CharcotMarieToothExt', 'const score = Math.round((0.4+Number(input.cmt_score||2)*0.08)*100)/100;'),
        ('SmallFiberNeuropathyExt', 'const score = Math.round((0.35+Number(input.sfn_score||5)*0.06)*100)/100;'),
        ('AutonomicNeuropathyExt', 'const score = Math.round((0.4+Number(input.comp_score||3)*0.08)*100)/100;'),
        ('HereditaryNeuropathyExt', 'const score = Math.round((0.35+Number(input.gene||1)*0.1)*100)/100;'),
        ('VasculiticNeuropathyExt', 'const score = Math.round((0.4+Number(input.vessel_count||2)*0.1)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext63': [
        ('PediatricPeripheralNeuropathyExt', 'const score = Math.round((0.3+Number(input.ncs_amp||2)*0.06)*100)/100;'),
        ('PediatricCIDPExt', 'const score = Math.round((0.4+Number(input.efns_score||3)*0.06)*100)/100;'),
        ('PediatricGuillainBarreExt', 'const score = Math.round((0.45+Number(input.egs_score||3)*0.08)*100)/100;'),
        ('PediatricCIDPVariantExt', 'const score = Math.round((0.4+Number(input.mfs||0)*0.15)*100)/100;'),
        ('PediatricDiabeticNeuropathyExt', 'const score = Math.round((0.3+Number(input.duration||5)*0.05)*100)/100;'),
        ('PediatricCMTExt', 'const score = Math.round((0.4+Number(input.cmt_score||2)*0.08)*100)/100;'),
        ('PediatricSmallFiberExt', 'const score = Math.round((0.35+Number(input.sfn_score||3)*0.08)*100)/100;'),
        ('PediatricAutonomicNeuropathyExt', 'const score = Math.round((0.4+Number(input.comp_score||2)*0.1)*100)/100;'),
        ('PediatricHereditaryNeuropathyExt', 'const score = Math.round((0.35+Number(input.gene||1)*0.12)*100)/100;'),
        ('PediatricVasculiticNeuropathyExt', 'const score = Math.round((0.4+Number(input.vessel_count||2)*0.1)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext63': [
        ('PediatricNerveSurgeryExt', 'const score = Math.round((0.4+Number(input.nerve_count||1)*0.1)*100)/100;'),
        ('PediatricNerveRepairExt', 'const score = Math.round((0.45+Number(input.gap_length||5)*0.04)*100)/100;'),
        ('PediatricNerveTransferExt', 'const score = Math.round((0.45+Number(input.transfer_count||1)*0.1)*100)/100;'),
        ('PediatricPlantarReleaseExt', 'const score = Math.round((0.35+Number(input.feet||2)*0.1)*100)/100;'),
        ('PediatricTendonTransferExt', 'const score = Math.round((0.4+Number(input.tendon_count||2)*0.08)*100)/100;'),
        ('PediatricTarsalTunnelExt', 'const score = Math.round((0.35+Number(input.tarsal_score||3)*0.08)*100)/100;'),
        ('PediatricNeurolysisExt', 'const score = Math.round((0.4+Number(input.nerve_count||1)*0.1)*100)/100;'),
        ('PediatricMuscleBiopsySurgExt', 'const score = Math.round((0.3+Number(input.sites||2)*0.08)*100)/100;'),
        ('PediatricSpinalCordDetetherExt', 'const score = Math.round((0.5+Number(input.tether_score||2)*0.08)*100)/100;'),
        ('PediatricCRMOOrthoticExt', 'const score = Math.round((0.3+Number(input.brace_count||2)*0.08)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3he.py -- PCC v3.173.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.173.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.173.0, P3-HE)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.173.0 / P3-HE']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3he_v3_173_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-HE 3.173.0: {count} files written")
