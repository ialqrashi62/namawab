import os
MODULES = ['pcc_neuro_ext77', 'pcc_pediatric_neuro_ext66', 'pcc_pediatric_surg_ext66']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T13:30:00Z"

FUNCS = {
    'pcc_neuro_ext77': [
        ('SpinalMuscularAtrophyExt', 'const score = Math.round((0.4+Number(input.motor_score||10)*0.04)*100)/100;'),
        ('SBMAExt', 'const score = Math.round((0.4+Number(input.repeats||40)*0.008)*100)/100;'),
        ('FSHDExt', 'const score = Math.round((0.35+Number(input.fshd_score||3)*0.08)*100)/100;'),
        ('MyotonicDystrophyExt', 'const score = Math.round((0.4+Number(input.repeats||50)*0.005)*100)/100;'),
        ('LimbGirdleMuscularDystrophyExt', 'const score = Math.round((0.4+Number(input.lgmd_score||3)*0.08)*100)/100;'),
        ('FacioscapulohumeralExt', 'const score = Math.round((0.35+Number(input.fsh_score||3)*0.08)*100)/100;'),
        ('InclusionBodyMyositisExt', 'const score = Math.round((0.4+Number(input.weak_score||3)*0.1)*100)/100;'),
        ('DermatomyositisExt', 'const score = Math.round((0.4+Number(input.skin_score||3)*0.1)*100)/100;'),
        ('PolymyositisExt', 'const score = Math.round((0.4+Number(input.cpk_level||500)*0.0005)*100)/100;'),
        ('MyastheniaGravisMyopathyExt', 'const score = Math.round((0.4+Number(input.achr_pct||0)*0.01)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext66': [
        ('PediatricSMAExt', 'const score = Math.round((0.4+Number(input.motor_score||10)*0.04)*100)/100;'),
        ('PediatricSBMAExt', 'const score = Math.round((0.4+Number(input.onset_yr||3)*0.1)*100)/100;'),
        ('PediatricFSHDExt', 'const score = Math.round((0.35+Number(input.fshd_score||2)*0.1)*100)/100;'),
        ('PediatricMyotonicDystrophyExt', 'const score = Math.round((0.4+Number(input.repeats||40)*0.005)*100)/100;'),
        ('PediatricLGMDExt', 'const score = Math.round((0.4+Number(input.lgmd_score||2)*0.1)*100)/100;'),
        ('PediatricFacioscapulohumeralExt', 'const score = Math.round((0.35+Number(input.fsh_score||2)*0.1)*100)/100;'),
        ('PediatricIBMExt', 'const score = Math.round((0.4+Number(input.weak_score||2)*0.12)*100)/100;'),
        ('PediatricDermatomyositisExt', 'const score = Math.round((0.4+Number(input.skin_score||2)*0.12)*100)/100;'),
        ('PediatricPolymyositisExt', 'const score = Math.round((0.4+Number(input.cpk_level||500)*0.0005)*100)/100;'),
        ('PediatricCongenitalMyopathyExt', 'const score = Math.round((0.4+Number(input.gene||0)*0.15)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext66': [
        ('PediatricSpinalFusionExt', 'const score = Math.round((0.4+Number(input.levels_fused||3)*0.07)*100)/100;'),
        ('PediatricGrowingRodExt', 'const score = Math.round((0.5+Number(input.lengthening||6)*0.05)*100)/100;'),
        ('PediatricScoliosisSurgeryExt', 'const score = Math.round((0.4+Number(input.cobb||50)*0.005)*100)/100;'),
        ('PediatricMuscleBiopsyExt', 'const score = Math.round((0.3+Number(input.sites||2)*0.08)*100)/100;'),
        ('PediatricTendonReleaseExt', 'const score = Math.round((0.3+Number(input.tendons||3)*0.06)*100)/100;'),
        ('PediatricGastrostomyExt', 'const score = Math.round((0.3+Number(input.feeding_score||3)*0.1)*100)/100;'),
        ('PediatricNissFundoplicationExt', 'const score = Math.round((0.3+Number(input.reflux_score||3)*0.1)*100)/100;'),
        ('PediatricTracheostomyExt', 'const score = Math.round((0.35+Number(input.vent_dependent||0)*0.2)*100)/100;'),
        ('PediatricVPShuntExt', 'const score = Math.round((0.4+Number(input.catheter_level||2)*0.08)*100)/100;'),
        ('PediatricNusinersenDrugExt', 'const score = Math.round((0.4+Number(input.doses||4)*0.05)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3hh.py -- PCC v3.176.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.176.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.176.0, P3-HH)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.176.0 / P3-HH']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3hh_v3_176_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-HH 3.176.0: {count} files written")
