import os
MODULES = ['pcc_neuro_ext79', 'pcc_pediatric_neuro_ext68', 'pcc_pediatric_surg_ext68']
ROOT = r'c:\Users\ice\Desktop\NMEDCALVSCODE\pcc'
MIG = os.path.join(ROOT, 'migrations')
TS = "2026-07-28T13:50:00Z"

FUNCS = {
    'pcc_neuro_ext79': [
        ('ObstructiveHydrocephalusExt', 'const score = Math.round((0.4+Number(input.thickness||5)*0.05)*100)/100;'),
        ('CommunicatingHydrocephalusExt', 'const score = Math.round((0.4+Number(input.opening_pressure||20)*0.012)*100)/100;'),
        ('NormalPressureHydrocephalusExt', 'const score = Math.round((0.5+Number(input.triad_score||2)*0.1)*100)/100;'),
        ('HydrocephalusProgrammableValveExt', 'const score = Math.round((0.4+Number(input.valve_setting||3)*0.05)*100)/100;'),
        ('EndoscopicThirdVentriculostomyExt', 'const score = Math.round((0.45+Number(input.success_score||80)*0.005)*100)/100;'),
        ('ChoroidPlexusCauterizationExt', 'const score = Math.round((0.5+Number(input.cauterization_score||80)*0.005)*100)/100;'),
        ('HydrocephalusShuntTapExt', 'const score = Math.round((0.3+Number(input.tap_response||1)*0.2)*100)/100;'),
        ('HydrocephalusCognitiveExt', 'const score = Math.round((0.3+Number(input.cog_score||20)*0.02)*100)/100;'),
        ('HydrocephalusRehabExt', 'const score = Math.round((0.3+Number(input.sessions||10)*0.03)*100)/100;'),
        ('HydrocephalusBiochemicalExt', 'const score = Math.round((0.3+Number(input.biomarker||100)*0.005)*100)/100;'),
    ],
    'pcc_pediatric_neuro_ext68': [
        ('PediatricObstructiveHydroExt', 'const score = Math.round((0.4+Number(input.thickness||4)*0.06)*100)/100;'),
        ('PediatricCommunicatingHydroExt', 'const score = Math.round((0.4+Number(input.opening_pressure||15)*0.015)*100)/100;'),
        ('PediatricNPHExt', 'const score = Math.round((0.5+Number(input.triad_score||1)*0.15)*100)/100;'),
        ('PediatricProgrammableValveExt', 'const score = Math.round((0.4+Number(input.valve_setting||3)*0.05)*100)/100;'),
        ('PediatricETVExt', 'const score = Math.round((0.45+Number(input.success_score||75)*0.005)*100)/100;'),
        ('PediatricCPCExt', 'const score = Math.round((0.5+Number(input.cauterization_score||75)*0.005)*100)/100;'),
        ('PediatricShuntTapExt', 'const score = Math.round((0.3+Number(input.tap_response||1)*0.2)*100)/100;'),
        ('PediatricHydroCognitiveExt', 'const score = Math.round((0.3+Number(input.cog_score||15)*0.025)*100)/100;'),
        ('PediatricHydroRehabExt', 'const score = Math.round((0.3+Number(input.sessions||8)*0.04)*100)/100;'),
        ('PediatricHydroBiomarkerExt', 'const score = Math.round((0.3+Number(input.biomarker||100)*0.005)*100)/100;'),
    ],
    'pcc_pediatric_surg_ext68': [
        ('PediatricShuntPlacementExt', 'const score = Math.round((0.4+Number(input.catheter_length||30)*0.01)*100)/100;'),
        ('PediatricETVSurgExt', 'const score = Math.round((0.45+Number(input.balloon_count||1)*0.1)*100)/100;'),
        ('PediatricSubgalealShuntExt', 'const score = Math.round((0.4+Number(input.drain_days||5)*0.05)*100)/100;'),
        ('PediatricShuntRevisionExt', 'const score = Math.round((0.4+Number(input.revision_count||1)*0.15)*100)/100;'),
        ('PediatricShuntRemovalExt', 'const score = Math.round((0.4+Number(input.months_in||12)*0.02)*100)/100;'),
        ('PediatricShuntExternalizationExt', 'const score = Math.round((0.4+Number(input.days_external||7)*0.04)*100)/100;'),
        ('PediatricShuntProgrammableExt', 'const score = Math.round((0.4+Number(input.setting_changes||3)*0.08)*100)/100;'),
        ('PediatricShuntAntibioticExt', 'const score = Math.round((0.3+Number(input.days_abx||10)*0.04)*100)/100;'),
        ('PediatricDrainInsertionExt', 'const score = Math.round((0.35+Number(input.drain_level||2)*0.08)*100)/100;'),
        ('PediatricThirdVentricleExplorationExt', 'const score = Math.round((0.5+Number(input.fenestration_count||2)*0.1)*100)/100;'),
    ],
}

def write_engine(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    funcs = FUNCS[mod]
    lines = ['// Auto-generated by gen_p3hj.py -- PCC v3.178.0','"use strict";', f"const TS = '{TS}';", ""]
    for fname, body in funcs:
        lines.append(f"function {fname}(input) {{ {body} return {{ version:'3.178.0', module:'{mod}', function:'{fname}', input, score, ts: TS }}; }}")
        lines.append("")
    lines.append("module.exports = {")
    for fname, _ in funcs:
        lines.append(f"  {fname},")
    lines.append("};")
    open(os.path.join(d, f'{mod}_engine.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "let passed=0;",
             "function assertEq(a,b,msg){if(a===b){passed++;}else if(typeof a==='object'&&JSON.stringify(a)===JSON.stringify(b)){passed++;}else{console.error('FAIL',msg,a,b);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod]:
        lines.append(f"assertEq({fname}({{}}).function, '{fname}', '{fname} basic');")
    lines.append(f"console.log('{mod} unit: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_integration_test(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated integration test','"use strict";',
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const makeDb = () => ({ records: [] });",
             "let passed=0;",
             "function assert(cond,msg){if(cond){passed++;}else{console.error('FAIL',msg);process.exit(1);}}",
             ]
    for fname, _ in FUNCS[mod][:5]:
        lines.append(f"{{ const r = {fname}({{}}); const db=makeDb(); db.records.push(r); assert(db.records.length===1,'{fname} persist'); }}")
    lines.append(f"console.log('{mod} integration: ' + passed + ' passed');")
    open(os.path.join(d, f'{mod}_integration_test.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_routes(mod):
    d = os.path.join(ROOT, mod)
    fl = [f for f,_ in FUNCS[mod]]
    lines = ['// Auto-generated routes','"use strict";',
             "const express = require('express');",
             "const {" + ", ".join(fl) + "} = require('./" + mod + "_engine');",
             "const router = express.Router();",
             "const db = [];",
             "function authenticate(req,res,next){return next();}",
             "router.get('/list', authenticate, (req,res)=>{ res.json([" + ", ".join([f"{{name:'{n}'}}" for n in fl]) + "]); });",
             "router.post('/call/:fn', authenticate, (req,res)=>{ const fn=req.params.fn; const allow=" + repr(fl) + "; if(!allow.includes(fn)) return res.status(400).json({error:'bad fn'}); const r=eval(fn)(req.body||{}); res.json(r); });",
             "router.post('/record', authenticate, (req,res)=>{ db.push(req.body); res.json({ok:true, n:db.length}); });",
             "module.exports = router;"]
    open(os.path.join(d, f'{mod}_routes.js'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_migration(mod):
    d = os.path.join(ROOT, mod); os.makedirs(d, exist_ok=True)
    lines = [f"-- Auto-generated migration for {mod} (v3.178.0, P3-HJ)",
             f"IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = '{mod}_records')",
             f"BEGIN CREATE TABLE {mod}_records (",
             "  id BIGINT IDENTITY(1,1) PRIMARY KEY,",
             "  tenant_id NVARCHAR(64) NOT NULL,",
             "  fn_name NVARCHAR(128) NOT NULL,",
             "  score DECIMAL(8,3) NULL,",
             "  payload NVARCHAR(MAX) NULL,",
             "  created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME())",
             f"  CREATE INDEX ix_{mod}_records_tenant_fn ON {mod}_records(tenant_id, fn_name);",
             "END GO"]
    open(os.path.join(d, f'{mod}_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

def write_package_migration():
    os.makedirs(MIG, exist_ok=True)
    lines = ['-- Combined package migration for v3.178.0 / P3-HJ']
    for m in MODULES:
        lines.append(f"IF OBJECT_ID('{m}_records') IS NULL CREATE TABLE {m}_records (id BIGINT IDENTITY(1,1) PRIMARY KEY, tenant_id NVARCHAR(64) NOT NULL, fn_name NVARCHAR(128) NOT NULL, score DECIMAL(8,3) NULL, payload NVARCHAR(MAX) NULL, created_at DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME());")
    open(os.path.join(MIG, 'p3hj_v3_178_0_up.sql'), 'w', encoding='utf-8').write('\n'.join(lines))

count=0
for m in MODULES:
    write_engine(m); count+=1
    write_test(m); count+=1
    write_integration_test(m); count+=1
    write_routes(m); count+=1
    write_migration(m); count+=1
write_package_migration(); count+=1
print(f"P3-HJ 3.178.0: {count} files written")
