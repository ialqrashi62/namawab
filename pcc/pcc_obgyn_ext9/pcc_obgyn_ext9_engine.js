// Auto-generated by gen_p3master.py -- PCC 3.237.0
"use strict";
const TS = '2026-07-29T14:00:00Z';
const VER = '3.237.0';

function EXT9AssessmentExt(input) {
  const score = Math.round((0.3 + (Number(input.ext9) || 0) * 0.25) * 100) / 100;
  return { version: VER, module: 'pcc_obgyn_ext9', function: 'EXT9AssessmentExt', input, score, ts: TS };
}

function EXT9ScoreExt(input) {
  const score = Math.round((0.3 + (Number(input.ext9) || 0) * 0.36) * 100) / 100;
  return { version: VER, module: 'pcc_obgyn_ext9', function: 'EXT9ScoreExt', input, score, ts: TS };
}

function EXT9StageExt(input) {
  const score = Math.round((0.3 + (Number(input.ext9) || 0) * 0.37) * 100) / 100;
  return { version: VER, module: 'pcc_obgyn_ext9', function: 'EXT9StageExt', input, score, ts: TS };
}

function EXT9PlanExt(input) {
  const score = Math.round((0.3 + (Number(input.ext9) || 0) * 0.33) * 100) / 100;
  return { version: VER, module: 'pcc_obgyn_ext9', function: 'EXT9PlanExt', input, score, ts: TS };
}

function EXT9RiskExt(input) {
  const score = Math.round((0.3 + (Number(input.ext9) || 0) * 0.34) * 100) / 100;
  return { version: VER, module: 'pcc_obgyn_ext9', function: 'EXT9RiskExt', input, score, ts: TS };
}

function EXT9DoseExt(input) {
  const score = Math.round((0.3 + (Number(input.ext9) || 0) * 0.35) * 100) / 100;
  return { version: VER, module: 'pcc_obgyn_ext9', function: 'EXT9DoseExt', input, score, ts: TS };
}

function EXT9FrequencyExt(input) {
  const score = Math.round((0.3 + (Number(input.ext9) || 0) * 0.26) * 100) / 100;
  return { version: VER, module: 'pcc_obgyn_ext9', function: 'EXT9FrequencyExt', input, score, ts: TS };
}

function EXT9DurationExt(input) {
  const score = Math.round((0.3 + (Number(input.ext9) || 0) * 0.22) * 100) / 100;
  return { version: VER, module: 'pcc_obgyn_ext9', function: 'EXT9DurationExt', input, score, ts: TS };
}

function EXT9FollowupExt(input) {
  const score = Math.round((0.3 + (Number(input.ext9) || 0) * 0.23) * 100) / 100;
  return { version: VER, module: 'pcc_obgyn_ext9', function: 'EXT9FollowupExt', input, score, ts: TS };
}

function EXT9OutcomeExt(input) {
  const score = Math.round((0.3 + (Number(input.ext9) || 0) * 0.19) * 100) / 100;
  return { version: VER, module: 'pcc_obgyn_ext9', function: 'EXT9OutcomeExt', input, score, ts: TS };
}

module.exports = {
  EXT9AssessmentExt,
  EXT9ScoreExt,
  EXT9StageExt,
  EXT9PlanExt,
  EXT9RiskExt,
  EXT9DoseExt,
  EXT9FrequencyExt,
  EXT9DurationExt,
  EXT9FollowupExt,
  EXT9OutcomeExt,
};