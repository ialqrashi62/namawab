// Auto-generated by gen_p3master.py -- PCC 3.262.0
"use strict";
const TS = '2026-07-29T14:00:00Z';
const VER = '3.262.0';

function EXT3AssessmentExt(input) {
  const score = Math.round((0.3 + (Number(input.ext3) || 0) * 0.25) * 100) / 100;
  return { version: VER, module: 'pcc_trauma_ext3', function: 'EXT3AssessmentExt', input, score, ts: TS };
}

function EXT3ScoreExt(input) {
  const score = Math.round((0.3 + (Number(input.ext3) || 0) * 0.36) * 100) / 100;
  return { version: VER, module: 'pcc_trauma_ext3', function: 'EXT3ScoreExt', input, score, ts: TS };
}

function EXT3StageExt(input) {
  const score = Math.round((0.3 + (Number(input.ext3) || 0) * 0.37) * 100) / 100;
  return { version: VER, module: 'pcc_trauma_ext3', function: 'EXT3StageExt', input, score, ts: TS };
}

function EXT3PlanExt(input) {
  const score = Math.round((0.3 + (Number(input.ext3) || 0) * 0.33) * 100) / 100;
  return { version: VER, module: 'pcc_trauma_ext3', function: 'EXT3PlanExt', input, score, ts: TS };
}

function EXT3RiskExt(input) {
  const score = Math.round((0.3 + (Number(input.ext3) || 0) * 0.34) * 100) / 100;
  return { version: VER, module: 'pcc_trauma_ext3', function: 'EXT3RiskExt', input, score, ts: TS };
}

function EXT3DoseExt(input) {
  const score = Math.round((0.3 + (Number(input.ext3) || 0) * 0.35) * 100) / 100;
  return { version: VER, module: 'pcc_trauma_ext3', function: 'EXT3DoseExt', input, score, ts: TS };
}

function EXT3FrequencyExt(input) {
  const score = Math.round((0.3 + (Number(input.ext3) || 0) * 0.26) * 100) / 100;
  return { version: VER, module: 'pcc_trauma_ext3', function: 'EXT3FrequencyExt', input, score, ts: TS };
}

function EXT3DurationExt(input) {
  const score = Math.round((0.3 + (Number(input.ext3) || 0) * 0.22) * 100) / 100;
  return { version: VER, module: 'pcc_trauma_ext3', function: 'EXT3DurationExt', input, score, ts: TS };
}

function EXT3FollowupExt(input) {
  const score = Math.round((0.3 + (Number(input.ext3) || 0) * 0.23) * 100) / 100;
  return { version: VER, module: 'pcc_trauma_ext3', function: 'EXT3FollowupExt', input, score, ts: TS };
}

function EXT3OutcomeExt(input) {
  const score = Math.round((0.3 + (Number(input.ext3) || 0) * 0.19) * 100) / 100;
  return { version: VER, module: 'pcc_trauma_ext3', function: 'EXT3OutcomeExt', input, score, ts: TS };
}

module.exports = {
  EXT3AssessmentExt,
  EXT3ScoreExt,
  EXT3StageExt,
  EXT3PlanExt,
  EXT3RiskExt,
  EXT3DoseExt,
  EXT3FrequencyExt,
  EXT3DurationExt,
  EXT3FollowupExt,
  EXT3OutcomeExt,
};