# Build artifacts
build/
.cache/

# Database files
*.db
*.bak

# Large data files
pharmacy_global_products_*.json
output.txt
drugs_export.txt
error_output.txt

# IDE / Agents
.agents/

# Credentials (NEVER commit)
algolia_creds.json
config.ini

# Environment secrets (NEVER commit real .env — only *.example placeholders are tracked)
.env
.env.*
!.env.example
!.env.*.example

# Node modules
node_modules/

# Generated Desktop Artifacts
*.exe
*.msi
*.dmg
*.AppImage
release/
releases/
dist-electron/
win-unpacked/
out/make/
src-tauri/target/
scratch/
skills-lock.json

# Database backups
docs/sql/*_backup.sql
docs/sql/*backup.sql
docs/sql/*backup*.sql
local_backups/
.local-backups/
backups/private/


# A3 KEK escrow package (owner-run output) — never commit
nama_kek_escrow*.enc
*.kek.escrow
# defensive backstop: any KEK escrow / DPAPI backup artifacts (owner-run output stays OUTSIDE repo)
*kek*.enc
*.escrow
*.escrow.enc
*dpapi*.backup
nama_kek*.dpapi

# external reference clones (not part of Jumanasoft IP)
.vendor/
.ai-brain/external-skills/
.agents/skills/
