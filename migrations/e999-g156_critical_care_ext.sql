-- TIER136 Critical Care (g156) - 20 tables
DO $$ BEGIN CREATE TABLE card_cath (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, procedure TEXT NOT NULL, access TEXT NOT NULL, contrast_ml INTEGER NOT NULL, fluoro_dose_gy DOUBLE PRECISION NOT NULL, door_to_balloon_min INTEGER NOT NULL, provider TEXT NOT NULL, performed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE card_cath ENABLE ROW LEVEL SECURITY; ALTER TABLE card_cath FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE card_stress (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, type TEXT NOT NULL, max_hr INTEGER NOT NULL, hr_recovery INTEGER NOT NULL, st_changes BOOLEAN NOT NULL, result TEXT NOT NULL, provider TEXT NOT NULL, performed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE card_stress ENABLE ROW LEVEL SECURITY; ALTER TABLE card_stress FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE card_echo (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, type TEXT NOT NULL, lvef INTEGER NOT NULL, lvidd DOUBLE PRECISION NOT NULL, tapse INTEGER NOT NULL, valve_disease TEXT NOT NULL, provider TEXT NOT NULL, performed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE card_echo ENABLE ROW LEVEL SECURITY; ALTER TABLE card_echo FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE card_device (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, device_type TEXT NOT NULL, battery_pct INTEGER NOT NULL, leads_impedance INTEGER NOT NULL, shocks_delivered BOOLEAN NOT NULL, provider TEXT NOT NULL, episode_summary TEXT NOT NULL, checked_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE card_device ENABLE ROW LEVEL SECURITY; ALTER TABLE card_device FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE card_ablation (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, target TEXT NOT NULL, energy TEXT NOT NULL, procedure_duration_min INTEGER NOT NULL, acute_success BOOLEAN NOT NULL, provider TEXT NOT NULL, performed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE card_ablation ENABLE ROW LEVEL SECURITY; ALTER TABLE card_ablation FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE str_stroke_alert (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, last_known_well_min INTEGER NOT NULL, nihss INTEGER NOT NULL, aspires INTEGER NOT NULL, lvo_suspected BOOLEAN NOT NULL, door_to_ct_min INTEGER NOT NULL, door_to_needle_min INTEGER NOT NULL, provider TEXT NOT NULL, alert_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE str_stroke_alert ENABLE ROW LEVEL SECURITY; ALTER TABLE str_stroke_alert FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE str_tpa (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, stroke_id TEXT NOT NULL, dose_mg DOUBLE PRECISION NOT NULL, weight_kg DOUBLE PRECISION NOT NULL, outcome TEXT NOT NULL, provider TEXT NOT NULL, bleeding_complication BOOLEAN NOT NULL, administered_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE str_tpa ENABLE ROW LEVEL SECURITY; ALTER TABLE str_tpa FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE str_thrombectomy (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, stroke_id TEXT NOT NULL, door_to_puncture_min INTEGER NOT NULL, tici_reperfusion INTEGER NOT NULL, technique TEXT NOT NULL, provider TEXT NOT NULL, post_mrs BOOLEAN NOT NULL, performed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE str_thrombectomy ENABLE ROW LEVEL SECURITY; ALTER TABLE str_thrombectomy FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE str_icp (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, method TEXT NOT NULL, icp_mmhg INTEGER NOT NULL, cpp_mmhg INTEGER NOT NULL, treatment_initiated BOOLEAN NOT NULL, provider TEXT NOT NULL, tier TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE str_icp ENABLE ROW LEVEL SECURITY; ALTER TABLE str_icp FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE str_milestone (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, day_post_stroke INTEGER NOT NULL, disposition TEXT NOT NULL, mrs INTEGER NOT NULL, barthel INTEGER NOT NULL, dysphagia_screened BOOLEAN NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE str_milestone ENABLE ROW LEVEL SECURITY; ALTER TABLE str_milestone FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE bur_assess (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, tbsa_pct INTEGER NOT NULL, depth TEXT NOT NULL, mechanism TEXT NOT NULL, inhalation_injury BOOLEAN NOT NULL, provider TEXT NOT NULL, assessed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE bur_assess ENABLE ROW LEVEL SECURITY; ALTER TABLE bur_assess FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE bur_fluid (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, burn_id TEXT NOT NULL, weight_kg DOUBLE PRECISION NOT NULL, tbsa_pct INTEGER NOT NULL, formula TEXT NOT NULL, rate_ml_hr INTEGER NOT NULL, urine_output_ml_hr INTEGER NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE bur_fluid ENABLE ROW LEVEL SECURITY; ALTER TABLE bur_fluid FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE bur_wound (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, burn_id TEXT NOT NULL, dressing TEXT NOT NULL, excision TEXT NOT NULL, grafting BOOLEAN NOT NULL, provider TEXT NOT NULL, site TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE bur_wound ENABLE ROW LEVEL SECURITY; ALTER TABLE bur_wound FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE bur_inhalation (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, burn_id TEXT NOT NULL, intubated BOOLEAN NOT NULL, cxr_grade INTEGER NOT NULL, co_poisoning BOOLEAN NOT NULL, cohb_pct DOUBLE PRECISION NOT NULL, provider TEXT NOT NULL, hyperbaric TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE bur_inhalation ENABLE ROW LEVEL SECURITY; ALTER TABLE bur_inhalation FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE bur_rehab (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, day_post_burn INTEGER NOT NULL, range_motion_pct INTEGER NOT NULL, contracture_risk INTEGER NOT NULL, graft_healed BOOLEAN NOT NULL, provider TEXT NOT NULL, pressure_garment TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE bur_rehab ENABLE ROW LEVEL SECURITY; ALTER TABLE bur_rehab FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tox_screen (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, specimen TEXT NOT NULL, substances_tested TEXT NOT NULL, detected_count INTEGER NOT NULL, substances_detected TEXT NOT NULL, provider TEXT NOT NULL, method TEXT NOT NULL, collected_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tox_screen ENABLE ROW LEVEL SECURITY; ALTER TABLE tox_screen FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tox_poisoning (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, toxin TEXT NOT NULL, ingestion_time_min INTEGER NOT NULL, dose_amount DOUBLE PRECISION NOT NULL, intent TEXT NOT NULL, severity TEXT NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tox_poisoning ENABLE ROW LEVEL SECURITY; ALTER TABLE tox_poisoning FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tox_antidote (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, pois_id TEXT NOT NULL, antidote TEXT NOT NULL, dose_mg DOUBLE PRECISION NOT NULL, effective BOOLEAN NOT NULL, provider TEXT NOT NULL, administered_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tox_antidote ENABLE ROW LEVEL SECURITY; ALTER TABLE tox_antidote FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tox_decon (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, pois_id TEXT NOT NULL, method TEXT NOT NULL, start_min INTEGER NOT NULL, contraindicated BOOLEAN NOT NULL, provider TEXT NOT NULL, performed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tox_decon ENABLE ROW LEVEL SECURITY; ALTER TABLE tox_decon FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tox_follow (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, pois_id TEXT NOT NULL, hour_post INTEGER NOT NULL, lab_value DOUBLE PRECISION NOT NULL, trend TEXT NOT NULL, provider TEXT NOT NULL, complications BOOLEAN NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tox_follow ENABLE ROW LEVEL SECURITY; ALTER TABLE tox_follow FORCE ROW LEVEL SECURITY;
