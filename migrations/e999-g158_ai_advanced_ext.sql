-- TIER138 AI Advanced (g158) - 20 tables
DO $$ BEGIN CREATE TABLE rag_chunk (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, document_id TEXT NOT NULL, text TEXT NOT NULL, chunker TEXT NOT NULL, chunk_size INTEGER NOT NULL, overlap INTEGER NOT NULL, embedding_model TEXT NOT NULL, chunked_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE rag_chunk ENABLE ROW LEVEL SECURITY; ALTER TABLE rag_chunk FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE rag_search (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, query TEXT NOT NULL, space TEXT NOT NULL, top_k INTEGER NOT NULL, min_score DOUBLE PRECISION NOT NULL, filter TEXT NOT NULL, searched_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE rag_search ENABLE ROW LEVEL SECURITY; ALTER TABLE rag_search FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE rag_rerank (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, query TEXT NOT NULL, candidate_count INTEGER NOT NULL, return_count INTEGER NOT NULL, model TEXT NOT NULL, score_threshold DOUBLE PRECISION NOT NULL, model_version TEXT NOT NULL, reranked_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE rag_rerank ENABLE ROW LEVEL SECURITY; ALTER TABLE rag_rerank FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE rag_query (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, user_id TEXT NOT NULL, query TEXT NOT NULL, context TEXT NOT NULL, answer TEXT NOT NULL, confidence DOUBLE PRECISION NOT NULL, citations TEXT NOT NULL, model TEXT NOT NULL, queried_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE rag_query ENABLE ROW LEVEL SECURITY; ALTER TABLE rag_query FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE rag_hallucination (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, rq_id TEXT NOT NULL, faithfulness DOUBLE PRECISION NOT NULL, relevance DOUBLE PRECISION NOT NULL, coverage DOUBLE PRECISION NOT NULL, verdict TEXT NOT NULL, feedback TEXT NOT NULL, checked_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE rag_hallucination ENABLE ROW LEVEL SECURITY; ALTER TABLE rag_hallucination FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE gen_fastq_qc (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, sample_id TEXT NOT NULL, platform TEXT NOT NULL, read_count INTEGER NOT NULL, read_length INTEGER NOT NULL, q30_pct DOUBLE PRECISION NOT NULL, gc_pct DOUBLE PRECISION NOT NULL, grade TEXT NOT NULL, qc_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE gen_fastq_qc ENABLE ROW LEVEL SECURITY; ALTER TABLE gen_fastq_qc FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE gen_alignment (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, sample_id TEXT NOT NULL, reference TEXT NOT NULL, aligner TEXT NOT NULL, mapped_pct DOUBLE PRECISION NOT NULL, coverage DOUBLE PRECISION NOT NULL, insert_size INTEGER NOT NULL, bam_path TEXT NOT NULL, aligned_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE gen_alignment ENABLE ROW LEVEL SECURITY; ALTER TABLE gen_alignment FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE gen_variant_call (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, sample_id TEXT NOT NULL, bam_path TEXT NOT NULL, caller TEXT NOT NULL, snp_count INTEGER NOT NULL, indel_count INTEGER NOT NULL, vcf_path TEXT NOT NULL, pipeline TEXT NOT NULL, called_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE gen_variant_call ENABLE ROW LEVEL SECURITY; ALTER TABLE gen_variant_call FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE gen_annotation (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, vcf_path TEXT NOT NULL, annotator TEXT NOT NULL, pathogenic_count INTEGER NOT NULL, vus_count INTEGER NOT NULL, benign_count INTEGER NOT NULL, report_path TEXT NOT NULL, annotated_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE gen_annotation ENABLE ROW LEVEL SECURITY; ALTER TABLE gen_annotation FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE gen_report (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, an_id TEXT NOT NULL, report_type TEXT NOT NULL, findings TEXT NOT NULL, actionable_count INTEGER NOT NULL, signoff TEXT NOT NULL, provider TEXT NOT NULL, reported_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE gen_report ENABLE ROW LEVEL SECURITY; ALTER TABLE gen_report FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tel_session_start (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, provider_id TEXT NOT NULL, session_type TEXT NOT NULL, platform TEXT NOT NULL, bandwidth_kbps INTEGER NOT NULL, latency_ms INTEGER NOT NULL, device_info TEXT NOT NULL, started_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tel_session_start ENABLE ROW LEVEL SECURITY; ALTER TABLE tel_session_start FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tel_recording (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, ss_id TEXT NOT NULL, recording_id TEXT NOT NULL, duration_sec INTEGER NOT NULL, format TEXT NOT NULL, encrypted BOOLEAN NOT NULL, size_mb INTEGER NOT NULL, storage_path TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tel_recording ENABLE ROW LEVEL SECURITY; ALTER TABLE tel_recording FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tel_streaming (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, ss_id TEXT NOT NULL, protocol TEXT NOT NULL, bitrate_kbps INTEGER NOT NULL, packets_total INTEGER NOT NULL, packets_lost INTEGER NOT NULL, jitter_ms INTEGER NOT NULL, quality TEXT NOT NULL, streamed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tel_streaming ENABLE ROW LEVEL SECURITY; ALTER TABLE tel_streaming FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tel_vitals (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, device_id TEXT NOT NULL, hr INTEGER NOT NULL, spo2 INTEGER NOT NULL, systolic INTEGER NOT NULL, diastolic INTEGER NOT NULL, temp DOUBLE PRECISION NOT NULL, respiratory_rate INTEGER NOT NULL, priority TEXT NOT NULL, streamed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tel_vitals ENABLE ROW LEVEL SECURITY; ALTER TABLE tel_vitals FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tel_session_end (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, ss_id TEXT NOT NULL, duration_sec INTEGER NOT NULL, disconnect_count INTEGER NOT NULL, summary TEXT NOT NULL, billed BOOLEAN NOT NULL, followup_scheduled BOOLEAN NOT NULL, provider TEXT NOT NULL, ended_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tel_session_end ENABLE ROW LEVEL SECURITY; ALTER TABLE tel_session_end FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE alert_smart (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, alert_id TEXT NOT NULL, patient_id TEXT NOT NULL, alert_type TEXT NOT NULL, severity TEXT NOT NULL, message TEXT NOT NULL, to_provider TEXT NOT NULL, delivery_channels TEXT NOT NULL, score DOUBLE PRECISION NOT NULL, acknowledged BOOLEAN NOT NULL, alert_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE alert_smart ENABLE ROW LEVEL SECURITY; ALTER TABLE alert_smart FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE alert_rule (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, rule_id TEXT NOT NULL, rule_name TEXT NOT NULL, trigger TEXT NOT NULL, condition TEXT NOT NULL, action TEXT NOT NULL, active BOOLEAN NOT NULL, fire_count INTEGER NOT NULL, provider TEXT NOT NULL, configured_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE alert_rule ENABLE ROW LEVEL SECURITY; ALTER TABLE alert_rule FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE alert_suppression (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, alert_id TEXT NOT NULL, reason TEXT NOT NULL, suppressed_by TEXT NOT NULL, silent BOOLEAN NOT NULL, duration_min INTEGER NOT NULL, provider TEXT NOT NULL, suppressed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE alert_suppression ENABLE ROW LEVEL SECURITY; ALTER TABLE alert_suppression FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE alert_escalation (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, alert_id TEXT NOT NULL, tier_level TEXT NOT NULL, escalated_to TEXT NOT NULL, escalation_time_min INTEGER NOT NULL, reason TEXT NOT NULL, provider TEXT NOT NULL, resolved BOOLEAN NOT NULL, escalated_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE alert_escalation ENABLE ROW LEVEL SECURITY; ALTER TABLE alert_escalation FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE alert_feedback (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, alert_id TEXT NOT NULL, feedback_type TEXT NOT NULL, provider TEXT NOT NULL, actionable BOOLEAN NOT NULL, reason TEXT NOT NULL, satisfaction_score INTEGER NOT NULL, improvement_suggestion TEXT NOT NULL, feedback_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE alert_feedback ENABLE ROW LEVEL SECURITY; ALTER TABLE alert_feedback FORCE ROW LEVEL SECURITY;
