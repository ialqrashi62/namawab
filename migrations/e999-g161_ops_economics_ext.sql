-- TIER141 Operations / Economics (g161) - 20 tables
DO $$ BEGIN CREATE TABLE ops_bed (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, unit TEXT NOT NULL, bed_type TEXT NOT NULL, room_id TEXT NOT NULL, expected_los INTEGER NOT NULL, isolation TEXT NOT NULL, provider TEXT NOT NULL, assigned_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE ops_bed ENABLE ROW LEVEL SECURITY; ALTER TABLE ops_bed FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE ops_staff (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, staff_id TEXT NOT NULL, shift_id TEXT NOT NULL, role TEXT NOT NULL, unit TEXT NOT NULL, shift TEXT NOT NULL, patient_ratio INTEGER NOT NULL, senior BOOLEAN NOT NULL, supervisor TEXT NOT NULL, provider TEXT NOT NULL, assigned_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE ops_staff ENABLE ROW LEVEL SECURITY; ALTER TABLE ops_staff FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE ops_utilization (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, unit TEXT NOT NULL, period TEXT NOT NULL, bed_count INTEGER NOT NULL, bed_days INTEGER NOT NULL, occupied_days INTEGER NOT NULL, occupancy_pct DOUBLE PRECISION NOT NULL, avg_los DOUBLE PRECISION NOT NULL, turnover INTEGER NOT NULL, provider TEXT NOT NULL, computed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE ops_utilization ENABLE ROW LEVEL SECURITY; ALTER TABLE ops_utilization FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE ops_housekeeping (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, task_id TEXT NOT NULL, room_id TEXT NOT NULL, task_type TEXT NOT NULL, priority TEXT NOT NULL, estimated_min INTEGER NOT NULL, assigned_to TEXT NOT NULL, status TEXT NOT NULL, provider TEXT NOT NULL, scheduled_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE ops_housekeeping ENABLE ROW LEVEL SECURITY; ALTER TABLE ops_housekeeping FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE ops_transport (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, order_id TEXT NOT NULL, patient_id TEXT NOT NULL, type TEXT NOT NULL, priority TEXT NOT NULL, mode TEXT NOT NULL, from_location TEXT NOT NULL, to_location TEXT NOT NULL, status TEXT NOT NULL, provider TEXT NOT NULL, ordered_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE ops_transport ENABLE ROW LEVEL SECURITY; ALTER TABLE ops_transport FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE sup_inventory (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, item_id TEXT NOT NULL, sku TEXT NOT NULL, name TEXT NOT NULL, category TEXT NOT NULL, quantity INTEGER NOT NULL, par_level INTEGER NOT NULL, reorder_point INTEGER NOT NULL, unit_cost DOUBLE PRECISION NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE sup_inventory ENABLE ROW LEVEL SECURITY; ALTER TABLE sup_inventory FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE sup_po (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, po_id TEXT NOT NULL, vendor_id TEXT NOT NULL, total_amount DOUBLE PRECISION NOT NULL, status TEXT NOT NULL, line_count INTEGER NOT NULL, requestor TEXT NOT NULL, approver TEXT NOT NULL, provider TEXT NOT NULL, ordered_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE sup_po ENABLE ROW LEVEL SECURITY; ALTER TABLE sup_po FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE sup_shortage (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, item_id TEXT NOT NULL, severity TEXT NOT NULL, days_until_stockout INTEGER NOT NULL, alternatives TEXT NOT NULL, recommendation TEXT NOT NULL, burn_rate_per_day INTEGER NOT NULL, provider TEXT NOT NULL, detected_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE sup_shortage ENABLE ROW LEVEL SECURITY; ALTER TABLE sup_shortage FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE sup_recall (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, recall_id TEXT NOT NULL, item_id TEXT NOT NULL, class TEXT NOT NULL, reason TEXT NOT NULL, units_affected INTEGER NOT NULL, units_recovered INTEGER NOT NULL, notifying_body TEXT NOT NULL, provider TEXT NOT NULL, recalled_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE sup_recall ENABLE ROW LEVEL SECURITY; ALTER TABLE sup_recall FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE sup_cost (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, item_id TEXT NOT NULL, purchase_cost DOUBLE PRECISION NOT NULL, usage_count INTEGER NOT NULL, waste_count INTEGER NOT NULL, waste_pct DOUBLE PRECISION NOT NULL, outcome_score INTEGER NOT NULL, recommendation TEXT NOT NULL, provider TEXT NOT NULL, analyzed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE sup_cost ENABLE ROW LEVEL SECURITY; ALTER TABLE sup_cost FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE hec_qaly (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, intervention_id TEXT NOT NULL, patient_id TEXT NOT NULL, qaly_gained DOUBLE PRECISION NOT NULL, cost DOUBLE PRECISION NOT NULL, timeframe TEXT NOT NULL, cost_per_qaly DOUBLE PRECISION NOT NULL, cost_effective TEXT NOT NULL, provider TEXT NOT NULL, computed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE hec_qaly ENABLE ROW LEVEL SECURITY; ALTER TABLE hec_qaly FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE hec_budget (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, scenario_id TEXT NOT NULL, scenario_name TEXT NOT NULL, year1_cost DOUBLE PRECISION NOT NULL, year5_cost DOUBLE PRECISION NOT NULL, population_affected INTEGER NOT NULL, cost_per_member DOUBLE PRECISION NOT NULL, cost_savings TEXT NOT NULL, provider TEXT NOT NULL, computed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE hec_budget ENABLE ROW LEVEL SECURITY; ALTER TABLE hec_budget FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE hec_value (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, program_id TEXT NOT NULL, program_type TEXT NOT NULL, spend DOUBLE PRECISION NOT NULL, quality_score INTEGER NOT NULL, outcome_score INTEGER NOT NULL, efficiency DOUBLE PRECISION NOT NULL, shared_savings DOUBLE PRECISION NOT NULL, provider TEXT NOT NULL, computed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE hec_value ENABLE ROW LEVEL SECURITY; ALTER TABLE hec_value FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE hec_payor (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, period TEXT NOT NULL, total_volume INTEGER NOT NULL, tier1_pct INTEGER NOT NULL, medicare_pct INTEGER NOT NULL, medicaid_pct INTEGER NOT NULL, commercial_pct INTEGER NOT NULL, self_pay_pct INTEGER NOT NULL, collected_pct INTEGER NOT NULL, provider TEXT NOT NULL, computed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE hec_payor ENABLE ROW LEVEL SECURITY; ALTER TABLE hec_payor FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE hec_price (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, cpt_code TEXT NOT NULL, negotiated_rate DOUBLE PRECISION NOT NULL, cash_rate DOUBLE PRECISION NOT NULL, min_rate DOUBLE PRECISION NOT NULL, max_rate DOUBLE PRECISION NOT NULL, payor_id TEXT NOT NULL, compliant BOOLEAN NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE hec_price ENABLE ROW LEVEL SECURITY; ALTER TABLE hec_price FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE vox_command (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, user_id TEXT NOT NULL, audio_id TEXT NOT NULL, transcript TEXT NOT NULL, intent TEXT NOT NULL, confidence DOUBLE PRECISION NOT NULL, extracted TEXT NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE vox_command ENABLE ROW LEVEL SECURITY; ALTER TABLE vox_command FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE vox_wake (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, device_id TEXT NOT NULL, wake_word TEXT NOT NULL, response TEXT NOT NULL, far_field_snr DOUBLE PRECISION NOT NULL, false_trigger BOOLEAN NOT NULL, context TEXT NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE vox_wake ENABLE ROW LEVEL SECURITY; ALTER TABLE vox_wake FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE vox_dictation (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, user_id TEXT NOT NULL, session_id TEXT NOT NULL, text TEXT NOT NULL, duration_sec INTEGER NOT NULL, word_count INTEGER NOT NULL, format TEXT NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE vox_dictation ENABLE ROW LEVEL SECURITY; ALTER TABLE vox_dictation FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE vox_biometric (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, user_id TEXT NOT NULL, audio_id TEXT NOT NULL, confidence DOUBLE PRECISION NOT NULL, verdict TEXT NOT NULL, speaker_id TEXT NOT NULL, text TEXT NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE vox_biometric ENABLE ROW LEVEL SECURITY; ALTER TABLE vox_biometric FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE vox_ambient (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, session_id TEXT NOT NULL, patient_id TEXT NOT NULL, provider_id TEXT NOT NULL, duration_sec INTEGER NOT NULL, speaker_count INTEGER NOT NULL, transcript TEXT NOT NULL, structured_note TEXT NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE vox_ambient ENABLE ROW LEVEL SECURITY; ALTER TABLE vox_ambient FORCE ROW LEVEL SECURITY;
