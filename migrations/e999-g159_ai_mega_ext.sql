-- TIER139 AI Mega (g159) - 20 tables
DO $$ BEGIN CREATE TABLE tri_trial (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, trial_id TEXT NOT NULL, title TEXT NOT NULL, sponsor TEXT NOT NULL, phase TEXT NOT NULL, target_enrollment INTEGER NOT NULL, inclusion TEXT NOT NULL, exclusion TEXT NOT NULL, status TEXT NOT NULL, created_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tri_trial ENABLE ROW LEVEL SECURITY; ALTER TABLE tri_trial FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tri_enrollment (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, trial_id TEXT NOT NULL, patient_id TEXT NOT NULL, consent TEXT NOT NULL, eligibility_score INTEGER NOT NULL, inclusion_met BOOLEAN NOT NULL, exclusion_met BOOLEAN NOT NULL, screening_id TEXT NOT NULL, provider TEXT NOT NULL, enrolled_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tri_enrollment ENABLE ROW LEVEL SECURITY; ALTER TABLE tri_enrollment FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tri_visit (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, trial_id TEXT NOT NULL, patient_id TEXT NOT NULL, visit_num INTEGER NOT NULL, visit_date TEXT NOT NULL, visit_type TEXT NOT NULL, adherence_pct INTEGER NOT NULL, adverse_events INTEGER NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tri_visit ENABLE ROW LEVEL SECURITY; ALTER TABLE tri_visit FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tri_ae (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, trial_id TEXT NOT NULL, patient_id TEXT NOT NULL, severity TEXT NOT NULL, ctcae_grade INTEGER NOT NULL, serious BOOLEAN NOT NULL, treatment_related BOOLEAN NOT NULL, expedited_report TEXT NOT NULL, provider TEXT NOT NULL, reported_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tri_ae ENABLE ROW LEVEL SECURITY; ALTER TABLE tri_ae FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE tri_outcome (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, trial_id TEXT NOT NULL, endpoint TEXT NOT NULL, result TEXT NOT NULL, p_value DOUBLE PRECISION NOT NULL, met BOOLEAN NOT NULL, report_path TEXT NOT NULL, provider TEXT NOT NULL, computed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE tri_outcome ENABLE ROW LEVEL SECURITY; ALTER TABLE tri_outcome FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE wear_device (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, device_id TEXT NOT NULL, patient_id TEXT NOT NULL, device_type TEXT NOT NULL, manufacturer TEXT NOT NULL, model TEXT NOT NULL, firmware TEXT NOT NULL, verified BOOLEAN NOT NULL, provider TEXT NOT NULL, registered_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE wear_device ENABLE ROW LEVEL SECURITY; ALTER TABLE wear_device FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE wear_telemetry (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, device_id TEXT NOT NULL, patient_id TEXT NOT NULL, metric_value DOUBLE PRECISION NOT NULL, metric_name TEXT NOT NULL, data_quality TEXT NOT NULL, battery_pct INTEGER NOT NULL, signal_strength INTEGER NOT NULL, captured_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE wear_telemetry ENABLE ROW LEVEL SECURITY; ALTER TABLE wear_telemetry FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE wear_anomaly (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, device_id TEXT NOT NULL, patient_id TEXT NOT NULL, anomaly_type TEXT NOT NULL, confidence DOUBLE PRECISION NOT NULL, severity TEXT NOT NULL, recommendation TEXT NOT NULL, provider TEXT NOT NULL, detected_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE wear_anomaly ENABLE ROW LEVEL SECURITY; ALTER TABLE wear_anomaly FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE wear_adherence (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, device_id TEXT NOT NULL, patient_id TEXT NOT NULL, target_min INTEGER NOT NULL, actual_min INTEGER NOT NULL, adherence_pct INTEGER NOT NULL, activity TEXT NOT NULL, period TEXT NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE wear_adherence ENABLE ROW LEVEL SECURITY; ALTER TABLE wear_adherence FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE wear_iot_alert (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, device_id TEXT NOT NULL, patient_id TEXT NOT NULL, alert_type TEXT NOT NULL, value DOUBLE PRECISION NOT NULL, threshold DOUBLE PRECISION NOT NULL, severity TEXT NOT NULL, delivery TEXT NOT NULL, provider TEXT NOT NULL, acknowledged BOOLEAN NOT NULL, alert_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE wear_iot_alert ENABLE ROW LEVEL SECURITY; ALTER TABLE wear_iot_alert FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE claim_submit (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, payer_id TEXT NOT NULL, provider_id TEXT NOT NULL, claim_type TEXT NOT NULL, amount_billed DOUBLE PRECISION NOT NULL, urgency TEXT NOT NULL, diagnosis_codes TEXT NOT NULL, procedure_codes TEXT NOT NULL, provider TEXT NOT NULL, submitted_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE claim_submit ENABLE ROW LEVEL SECURITY; ALTER TABLE claim_submit FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE claim_adj (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, claim_id TEXT NOT NULL, adjudication TEXT NOT NULL, amount_approved DOUBLE PRECISION NOT NULL, amount_patient_resp DOUBLE PRECISION NOT NULL, reason_codes TEXT NOT NULL, processing_time_hr INTEGER NOT NULL, provider TEXT NOT NULL, adjudicated_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE claim_adj ENABLE ROW LEVEL SECURITY; ALTER TABLE claim_adj FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE claim_fraud (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, claim_id TEXT NOT NULL, fraud_score DOUBLE PRECISION NOT NULL, risk_tier TEXT NOT NULL, red_flags TEXT NOT NULL, recommendation TEXT NOT NULL, historical_match DOUBLE PRECISION NOT NULL, provider TEXT NOT NULL, scored_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE claim_fraud ENABLE ROW LEVEL SECURITY; ALTER TABLE claim_fraud FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE claim_appeal (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, claim_id TEXT NOT NULL, appeal_level TEXT NOT NULL, appeal_reason TEXT NOT NULL, days_to_submit INTEGER NOT NULL, supporting_docs TEXT NOT NULL, status TEXT NOT NULL, provider TEXT NOT NULL, filed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE claim_appeal ENABLE ROW LEVEL SECURITY; ALTER TABLE claim_appeal FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE claim_remit (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, claim_id TEXT NOT NULL, era_eob TEXT NOT NULL, amount_paid DOUBLE PRECISION NOT NULL, amount_adjustment DOUBLE PRECISION NOT NULL, patient_responsibility DOUBLE PRECISION NOT NULL, provider TEXT NOT NULL, posted_at TEXT NOT NULL, era_path TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE claim_remit ENABLE ROW LEVEL SECURITY; ALTER TABLE claim_remit FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE rob_plan (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, patient_id TEXT NOT NULL, plan_id TEXT NOT NULL, platform TEXT NOT NULL, procedure TEXT NOT NULL, estimated_time_min INTEGER NOT NULL, surgeon TEXT NOT NULL, ai_assisted BOOLEAN NOT NULL, provider TEXT NOT NULL, planned_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE rob_plan ENABLE ROW LEVEL SECURITY; ALTER TABLE rob_plan FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE rob_instrument (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, session_id TEXT NOT NULL, instrument_id TEXT NOT NULL, instrument TEXT NOT NULL, used BOOLEAN NOT NULL, duration_used_min INTEGER NOT NULL, surgeon_id TEXT NOT NULL, status TEXT NOT NULL, rfid_tracked BOOLEAN NOT NULL, provider TEXT NOT NULL, tracked_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE rob_instrument ENABLE ROW LEVEL SECURITY; ALTER TABLE rob_instrument FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE rob_ai_assist (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, session_id TEXT NOT NULL, task TEXT NOT NULL, confidence DOUBLE PRECISION NOT NULL, guidance TEXT NOT NULL, accepted BOOLEAN NOT NULL, override BOOLEAN NOT NULL, provider TEXT NOT NULL, timestamp TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE rob_ai_assist ENABLE ROW LEVEL SECURITY; ALTER TABLE rob_ai_assist FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE rob_motion (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, session_id TEXT NOT NULL, tremor_amplitude_mm DOUBLE PRECISION NOT NULL, path_length_mm DOUBLE PRECISION NOT NULL, smoothness_score DOUBLE PRECISION NOT NULL, hand TEXT NOT NULL, surgeon_id TEXT NOT NULL, skill_score INTEGER NOT NULL, feedback TEXT NOT NULL, provider TEXT NOT NULL, analyzed_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE rob_motion ENABLE ROW LEVEL SECURITY; ALTER TABLE rob_motion FORCE ROW LEVEL SECURITY;
DO $$ BEGIN CREATE TABLE rob_post_op (id SERIAL PRIMARY KEY, tenant_id TEXT NOT NULL, session_id TEXT NOT NULL, ebl_ml INTEGER NOT NULL, los_days INTEGER NOT NULL, pain_score INTEGER NOT NULL, clavien_dindo TEXT NOT NULL, console_time_min INTEGER NOT NULL, robot_dock_time_min INTEGER NOT NULL, surgeon_id TEXT NOT NULL, provider TEXT NOT NULL, recorded_at TIMESTAMPTZ DEFAULT NOW()); EXCEPTION WHEN duplicate_table THEN NULL; END $$;
ALTER TABLE rob_post_op ENABLE ROW LEVEL SECURITY; ALTER TABLE rob_post_op FORCE ROW LEVEL SECURITY;
