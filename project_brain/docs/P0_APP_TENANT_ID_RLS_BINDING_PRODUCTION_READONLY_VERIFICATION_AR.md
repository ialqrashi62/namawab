# P0 ربط app.tenant_id — 04 التحقق read-only من الإنتاج (Production Read-only Verification)

> التاريخ: 2026-06-20 | **قراءة فقط، لا كتابة، لا DDL، لا نشر.** بمستخدم التطبيق `nama_medical_app` (غير superuser) على الإنتاج.

## 1. هدف التحقق
إثبات أن آلية الربط (ضبط `app.tenant_id` على الاتصال قبل الاستعلام) تجعل الجداول المحمية بـ FORCE RLS تعمل بشكل صحيح من خلال دور التطبيق الفعلي، وأن العزل يُفرَض على مستوى قاعدة البيانات.

## 2. النتائج (جدول patients — به 3 صفوف لمستأجر 1)

| الحالة | عدد صفوف patients كما يراها `nama_medical_app` | التفسير |
| ------ | :---------------------------------------------: | ------- |
| بدون ضبط `app.tenant_id` | **0** | RLS تحجب (المسلك الحالي للإنتاج غير المنشور عليه الإصلاح) |
| `app.tenant_id = 1` | **3** | بيانات المستأجر 1 تظهر بعد الربط ✅ |
| `app.tenant_id = 999` (مستأجر غير موجود) | **0** | عزل: المستأجر الآخر لا يرى شيئاً ✅ |
| إعادة الضبط إلى `''` | **0** | لا تسرّب بعد إعادة الضبط ✅ |

## 3. الاستنتاج
- **إنفاذ RLS صحيح**: بدون السياق الصحيح لا تظهر بيانات (حماية فعلية)، ومع `tenant_id=1` تظهر بيانات المستأجر 1 فقط، ومع مستأجر آخر لا شيء.
- **آلية الربط التي يطبّقها الكود الجديد تحل الحاجز**: ضبط `app.tenant_id` (كما يفعل الـ wrapper تلقائياً) يعيد الرؤية الصحيحة مع الحفاظ على العزل.
- **التحقق read-only فقط** — لم تتغيّر أي بيانات/مخطط، ولم يُنشر الكود بعد.

## 4. ملاحظة حرجة عن حالة الإنتاج الحالية
الإنتاج لا يزال يشغّل الكود **القديم** (بلا ربط) → التطبيق على الإنتاج **ما زال يرى 0 صف** في الجداول الـ13 حتى يُنشر الإصلاح. النشر خطوة منفصلة محكومة (تحتاج موافقة) — انظر تقرير الإغلاق.

`PRODUCTION_READONLY_VERIFICATION_COMPLETE — MECHANISM PROVEN, NOT YET DEPLOYED`
