# Wave 0B — قرار تعديلات namaweb الموازية (R17)

> 2026-06-22 | فحص قراءة-فقط، لم تُلمس التعديلات إطلاقاً. لا commit/stash/revert.

## الوضع
```text
DIRTY_FILES: namaweb/server.js, namaweb/public/js/app.js
LINES_CHANGED: 664 insertions, 10 deletions (2 ملفات)
NAMAWEB_HEAD = gitlink = bc24a47 (التعديلات working-tree فقط، غير ملتزَمة)
LIKELY_OWNER: الجلسة الموازية R17 (علامات حراساتي — BLOCKED_USER_CREATE/requireRole('hr')/CREATE_EMPLOYEE/rls_daily_close — غائبة عن الـdiff)
```

## الأثر
```text
CAN_DEPLOY_APP_CODE_NOW: NO
السبب: أي pm2 restart سيحمّل كود الجلسة الموازية غير المكتمل/غير المُراجَع ⇒ خطر استقرار.
```

## الخيارات الآمنة
| # | الخيار | الأثر | التوصية |
|---|---|---|---|
| A | الجلسة الموازية تلتزم عملها (commit) | يصبح الفرع نظيفاً، النشر ممكن بعد مراجعة | **مفضّل** إن كان عملها جاهزاً |
| B | الجلسة الموازية تعمل stash | يجمّد عملها مؤقتاً، الفرع نظيف | جيّد لو غير جاهز |
| C | إنشاء فرع لعمل R17 ثم العودة لنظيف | يحفظ عملها على فرع منفصل | جيّد للعزل |
| D | المالك يتجاهلها صراحةً (discard) | فقدان 664 سطراً | **فقط بقرار مالك صريح** — خطر فقدان عمل |

## القرار الموصى
**الخيار A أو C** بالتنسيق مع الجلسة الموازية — لا تُتّخذ من طرفي. **لا تجاهل (D) بلا قرار مالك صريح** (قد يكون عملاً مهمّاً قيد التطوير). حتى يُحسم: **`APP_CODE_DEPLOY_ALLOWED: NO`**.

## ملاحظة
gitlink الأب (bc24a47) ثابت؛ commits الأب لا تمسّ الـsubmodule. عملهم محفوظ على القرص ولم يتأثر بأي عمل لي.
