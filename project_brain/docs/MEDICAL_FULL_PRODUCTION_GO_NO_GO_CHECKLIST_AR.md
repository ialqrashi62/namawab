# قائمة تدقيق وجاهزية العبور للإنتاج (Production Go/No-Go Checklist)
## نظام نما الطبي (NamaMedical) - مرحلة التخطيط للترقية والعبور للإنتاج

توثق هذه القائمة البنود الفنية والتشغيلية الإلزامية التي يجب مراجعتها وتأكيد جاهزيتها بالكامل قبل اتخاذ قرار العبور والترقية للإنتاج الكامل (Go Decision).

---

### 1. جدول التحقق من الجاهزية الفنية (Go/No-Go Matrix)

يجب استيفاء وتأكيد جميع البنود التالية للقراءة فقط قبل المضي قدماً:

| البند التشغيلي المستهدف | الوصف والمعايير المطلوبة | الحالة (Ready / Not Ready) | آلية التحقق والتوثيق للقراءة فقط |
| :--- | :--- | :---: | :--- |
| **جاهزية النطاق (DNS Ready)** | سجلات A و CNAME موجهة، وتخفيض قيمة الـ TTL لـ 300 ثانية. | **Ready** (مخطط) | مطابقة سجلات أسماء النطاق وحالة الانتشار |
| **جاهزية التشفير (HTTPS Ready)** | تهيئة توجيه Nginx، والتحقق المسبق من Certbot، وتفعيل HSTS. | **Ready** (مخطط) | فحص ترويسات الأمان واستجابة Let's Encrypt |
| **جاهزية Redis للجلسات** | خادم مستقل مؤمن برمز سري ونشط دون تراجع MemoryStore. | **Ready** (مخطط) | تشغيل `redis-cli ping` وقراءة سجلات الخادم |
| **النسخ الاحتياطي لـ DB** | أخذ النسخة الثنائية، وحفظها خارج Git وتمرير تمرين الاستعادة. | **Ready** (مخطط) | تأكيد وجود الملف ومطابقة عدد الجداول لـ 148 |
| **جاهزية التراجع (Rollback Ready)** | إعداد وتأكيد أوامر استرداد الكود وقاعدة البيانات التلقائية. | **Ready** (مخطط) | تجربة سكربتات التراجع محلياً على Staging |
| **ملف التكوين البيئي (env)** | بناء ملف `.env` الإنتاجي خالي من الأسرار والمسارات الصلبة. | **Ready** (مخطط) | تدقيق أمني للملف ومراجعة الصلاحيات (600) |
| **إدارة الخدمة (PM2 Ready)** | تهيئة خدمة `nama-web-prod` وتمكين بدء التشغيل التلقائي مع النظام. | **Ready** (مخطط) | التحقق من تكوين `pm2 startup` والعمليات |
| **نقطة الصحة (Health Ready)** | تفعيل واختبار المسار `/api/health` وإرجاع رمز UP و 200 OK. | **Ready** (مخطط) | إرسال طلب curl وقراءة بيانات المكونات |
| **اختبار الدخان (Smoke Ready)** | نجاح سيناريوهات القبول والوصول للأقسام والتحقق من IDOR. | **Ready** (مخطط) | تشغيل اختبارات القبول الموضعية والوظيفية |
| **إنفاذ الـ RLS بالكامل** | RLS و FORCE RLS نشطة لـ 13 جدولاً ووجود 35 سياسة حماية. | **Ready** (مخطط) | الاستعلام من جداول النظام `pg_class` و `pg_policies` |
| **نظام المراقبة والإنذار** | تفعيل رصد المؤشرات للموارد (Prometheus) وضبط قنوات التصعيد. | **Ready** (مخطط) | إطلاق تنبيه تجريبي وتأكيد وصول الإشارات |
| **موافقة المالك الصريحة** | الحصول على موافقة بشرية خطية وصريحة وموثقة للبدء بالعبور. | **Not Ready** | تفعيل بوابة الموافقة الإدارية اللاحقة |

---

### 2. معايير القرار النهائي (Go/No-Go Decision Criteria)

1. **القرار: GO**:
   - يتم اعتماده فقط في حال كانت جميع البنود السابقة الـ 12 حالتها **Ready** (مع الحصول على موافقة المالك المكتوبة والصريحة).
2. **القرار: NO-GO**:
   - يتم اعتماده فوراً في حال كانت هناك حالة واحدة فقط **Not Ready** لأي بند فني أو أمني، ويتم إيقاف عملية النشر وإبقاء النظام على بيئة Staging الآمنة.

---
**الحالة**: **PLANNING_CHECKLIST_DOCUMENTED**
**حالة القرار الفعال**: **NO_GO_FOR_NOW** (بانتظار موافقة التشغيل والعبور النهائي للإنتاج)
**حالة البوابة**: **PASS**
