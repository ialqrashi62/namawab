# تقرير اكتشاف الجداول المتبقية عالية الخطورة (Remaining High-Risk Table Discovery Report)
## نظام نما الطبي - الدفعة الرابعة لتفعيل RLS التدريجي

---

### 1. ملخص الكشف والتدقيق (Discovery Summary)

تم تشغيل عملية الكشف التفصيلي وفحص البيانات والجداول المتبقية وعالية الخطورة في قاعدة البيانات الاستباقية (Staging Database) للوقوف على هيكليتها ومدى توزيع معرفات المستأجرين فيها. شمل الفحص 9 جداول متبقية مؤجلة من الدفعات السابقة بالإضافة لكتالوجات ومخزون الأدوية.

---

### 2. نتائج الفحص التفصيلية للجداول (Audit Table Metrics)

فيما يلي تفصيل نتائج الكشف واستعلام البيانات عن الجداول التسعة المستهدفة:

1. **pharmacy_prescriptions_queue** (طابور صرف الوصفات بالصيدلية):
   - يحتوي `tenant_id` (نعم)، `patient_id` (نعم)، `facility_id` (لا).
   - عدد السجلات: 0 سجلات.
   - الجاهزية: جاهز للتفعيل.
2. **pharmacy_sales** (مبيعات الصيدلية):
   - يحتوي `tenant_id` (نعم)، `patient_id` (نعم)، `facility_id` (لا).
   - عدد السجلات: 0 سجلات.
   - الجاهزية: جاهز للتفعيل.
3. **lab_results** (نتائج المختبر):
   - يحتوي `tenant_id` (نعم)، `patient_id` (لا)، `facility_id` (نعم).
   - عدد السجلات: 0 سجلات.
   - الجاهزية: جاهز للتفعيل.
4. **emergency_beds** (أسرة الطوارئ):
   - يحتوي `tenant_id` (نعم)، `patient_id` (لا)، `facility_id` (لا).
   - عدد السجلات: 8 صفوف (كلها تتبع `tenant_id = 1` دون أي قيم فارغة).
   - الجاهزية: جاهز للتفعيل.
5. **insurance_claims** (مطالبات التأمين):
   - يحتوي `tenant_id` (نعم)، `patient_id` (لا)، `facility_id` (نعم).
   - عدد السجلات: 3 صفوف (كلها تتبع `tenant_id = 1` دون أي قيم فارغة).
   - الجاهزية: جاهز للتفعيل.
6. **medications** (الأدوية):
   - يحتوي `tenant_id` (لا).
   - عدد السجلات: 0 سجلات.
   - الجاهزية: تفتقد لعمود العزل (مؤجل لحين إعادة الهيكلة).
7. **lab_samples** (عينات المختبر):
   - يحتوي `tenant_id` (لا).
   - عدد السجلات: 0 سجلات.
   - الجاهزية: تفتقد لعمود العزل (مؤجل لحين إعادة الهيكلة).
8. **lab_tests_catalog** (كتالوج تحاليل المختبر):
   - يحتوي `tenant_id` (لا).
   - عدد السجلات: 455 صفاً.
   - الجاهزية: كتالوج عام مشترك لجميع المستأجرين (مستثنى من RLS).
9. **radiology_catalog** (كتالوج الأشعة):
   - يحتوي `tenant_id` (لا).
   - عدد السجلات: 305 صفاً.
   - الجاهزية: كتالوج عام مشترك لجميع المستأجرين (مستثنى من RLS).

---

### 3. حوكمة وأمان البيانات

* **السرية وعدم الكشف**: تم التأكد من خلو كافة الجداول من أي بيانات تعريف شخصية حقيقية للمرضى أو تفاصيل مالية فعلية للعيادات، والبيانات الحالية وهمية 100% وتستخدم للاختبار الفني فقط.
* **الترميز اللغوي**: التقرير والبيانات خالية من Mojibake وتدعم العربية بنسبة 100%.

---
STATUS:
  MEDICAL_GRADUAL_RLS_BATCH4_DISCOVERY_COMPLETED
