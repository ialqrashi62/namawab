# تقرير نشر كود نسخة الإطلاق وتشغيل الخدمة (Cutover Deployment Report)
## نظام نما الطبي (NamaMedical) - مرحلة التنفيذ والعبور للإنتاج

يوثق هذا التقرير خطوات نشر الكود لنسخة الإطلاق المعتمدة وإعادة تشغيل الخدمة الطبية تحت إدارة PM2.

---

## 1. تفاصيل نسخة الإطلاق والتحقق من Git (Code Checkout)

* **الالتزام الأب المعتمد (Parent HEAD)**: `dfa2e77994192e4631e90570c41414c4ed5437f3`
* **الالتزام الفرعي لـ namaweb**: `7495fd53f4f24117741a9dd91de5b5ec19f4f248`
* **الحالة التشغيلية للمستودع**: تم التحقق من نجاح عملية Checkout وتطابق ملفات الكود محلياً مع نسخة الإطلاق دون أي تعارضات أو تعديلات غير متتبعة.

---

## 2. تشغيل وضبط الخدمة الطبية تحت PM2 (Service Status)

* **اسم العملية الموحد**: `nama-web`
* **الحالة التشغيلية**: `online` (نشط وقيد التشغيل بنجاح).
* **معرف العملية (PID)**: `15264`
* **المنفذ المستهدف**: `3000`
* **وضع التشغيل**: وضع الإنتاج الفعلي (`production`).

---

## 3. الفحص والمراقبة الأولية وقت التشغيل (Runtime Observation)

تم فحص سجلات تشغيل التطبيق (Runtime Logs) الناتجة عن PM2 فور التشغيل وتم استخلاص الملاحظات التالية:
- **ربط Redis**: نجح التطبيق في الاتصال بـ Redis لإدارة الجلسات دون أي تراجع للميموري ستور.
- **استهلاك الموارد**: استقرار تام في استهلاك الذاكرة (117.9 MB) ومعالج الخادم (أقل من 5%).
- **الاستجابة لطلبات الصحة (Health Check)**: يستجيب الخادم للطلبات الخارجية بنجاح كامل وبرمز الحالة `200 OK` واستجابة `{"status":"UP"}`.
- **خلو السجلات من الأسرار**: تم التأكد من عدم وجود أي كلمات مرور أو سلاسل اتصال مكشوفة داخل السجلات.
