# تقرير جاهزية المراقبة التشغيلية (Operational Monitoring Readiness Report)
## نظام نما الطبي (NamaMedical)

مستند يوثق مؤشرات الأداء، حماية خادم الويب، الجدار الناري، وشهادات الأمان لبيئة الـ Staging.

---

### 1. مؤشرات صحة الخدمات والخادم (Server & Services Health)

* **مدير العمليات PM2**:
  * حالة العملية `nama-medical-erp`: `ONLINE` (نشطة).
  * استهلاك الميموري: `65.7MB` (ضمن الحدود الطبيعية والآمنة).
  * معدل إعادة التشغيل (Restart Policy): مهيأ للمحاولة التلقائية عند حدوث انهيار برمي.
* **قاعدة البيانات ونظام الملفات**:
  * خدمة `postgresql` مستقرة ومتاحة محلياً فقط.
  * مساحة القرص الصلب كافية ومحمية ببروتوكولات التطهير الدوري للسجلات والنسخ القديمة.

---

### 2. الشبكة والأمن وجدار الحماية (Firewall & Network Security)

* **الجدار الناري UFW**: نشط ومضبوط بحظر كافة المنافذ الواردة باستثناء:
  * المنفذ `22` (SSH للوصول الإداري الآمن).
  * المنفذ `80` (HTTP لتحويل الزوار تلقائياً لـ HTTPS).
  * المنفذ `443` (HTTPS الآمن للتطبيق).
* **إدارة شهادات الأمان Certbot**:
  * الإصدار المتاح: `1.21.0`.
  * النطاق الموثق: `jumanasoft.com`.
  * التجديد التلقائي لشهادة SSL: نشط ويعمل بشكل دوري بواسطة Certbot systemd timer.
* **شريط تحذير بيئة Staging**: نشط ويظهر بصيغة تأمين HTTPS: `HTTPS-secured staging environment - Not production ready`.
