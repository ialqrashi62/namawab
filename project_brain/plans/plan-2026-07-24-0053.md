# خطة PLANNER v5.0 — بالاعتماد على الفجوات الفعلية
**المسح المرجعي**: `project_brain/scans/scan-2026-07-24-0053.json`
**التاريخ**: 2026-07-24
**المرجع الأعلى**: [AGENTS.md](../../AGENTS.md) (13 سكة أمان) — **يلغي** أي تعارض مع البرومبت v5.0

---

## ملخص تنفيذي (Executive Summary)

المشروع **ناضج جداً** (11,252 ملف · 597 اختبار · 86 skill · كل الـ Gates مغلقة ما عدا GATE 9).
**لا حاجة لـ "بناء قسم جديد"** — بدلاً من ذلك، الخطة تركّز على **3 فجوات حرجة فعلية** تم التحقق منها قرائياً:

| # | الفجوة | المصدر الموثّق | الأولوية | جهد تقديري |
|---|---|---|---|---|
| **T1** | RAG-grounded AI Copilot fix **جاهز للنشر على الإنتاج** ولم يُنفَّذ بعد (sandbox لا يصل لـ Hetzner) | CHANGELOG 2026-07-23 + `PHASE_RAG_LIVE_DEPLOY_2026-07-23_AR.md` | 🔴 **P0** | 30 دقيقة deploy من جهاز عنده SSH |
| **T2** | تدوير الأسرار الفعلي (PHASE 1B) — جاهز، لكن **المالك فقط** يستطيع التنفيذ | `PHASE_1B_*_AR.md` §4 | 🟠 **P0 (مالك)** | 15 دقيقة (5 secrets) |
| **T3** | GATE 9 (ZATCA UBL XAdES) — **محظور** على شهادة CSID حقيقية | `GATE9_ZATCA_UBL_XADES_BLOCKED_AR.md` | 🟡 **P2 (خارجي)** | رفع الحظر يتطلب بيانات اعتماد ZATCA |

> **التوصية**: التركيز على **T1** (RAG deploy) و **T2** (تدوير أسرار) لأنهما قابلان للتنفيذ **الآن** ويمثّلان قيمة مباشرة.

---

## المهام المقترحة (3 مهام فقط — التزم بالحد الأقصى حسب البرومبت)

### 🎯 TASK 1 — P0: نشر RAG-grounded AI Copilot Fix على الإنتاج
**المصدر**: `CHANGELOG.md` السطر 7-13 + `PHASE_RAG_LIVE_DEPLOY_2026-07-23_AR.md`
**المخرجات الجاهزة** (تم التحقق):
- ملف مُجهَّز: `namaweb/.deploy_staging_2026-07-23_ragfix/clinical_knowledge_rag.js` (6,005 bytes، `node --check` ✅)
- دليل نشر كامل: `ops/live_deploy/DEPLOY_HANDBOOK_2026-07-23_RAG_GROUNDED.md` (13 قسم، zero-downtime، rollback <30s)
- قالب إغلاق: `docs/PHASE_RAG_LIVE_DEPLOY_2026-07-23_AR.md`

**الخطوات**:
1. من جهاز عنده SSH إلى Hetzner (204.168.144.74):
   ```bash
   scp namaweb/.deploy_staging_2026-07-23_ragfix/clinical_knowledge_rag.js namaweb@server:/tmp/
   ssh namaweb@server 'sudo cp /tmp/clinical_knowledge_rag.js /var/www/namaweb/ && pm2 reload nama-medical-erp'
   ```
2. التحقق: `curl https://jumanasoft.com/api/ai/status` → `live: true, model: gpt-4, rag_grounded: true`
3. إغلاق: املأ `PHASE_RAG_LIVE_DEPLOY_2026-07-23_AR.md` وادمج في CHANGELOG

**سكك الأمان المعنية** (لا تنتهك أياً منها):
- ✅ Rail 3 (Phase-by-Phase) — جاهز للنشر، فقط تنفيذه
- ✅ Rail 5 (Backwards compat) — `+22 / -3 lines`، آمن
- ⚠️ Rail 1 (default to wait) — **بحاجة موافقة المالك قبل النشر**

**الوكلاء المشاركون**: BACKEND (نشر) + QA (تحقق بعد النشر)

---

### 🎯 TASK 2 — P0 (مالك فقط): تدوير الأسرار الـ 5 الفعلية
**المصدر**: `PHASE_1B_TRACKED_SECRET_REDACTION_REPORT_AR.md` §4

| # | السر | المكان السابق | الإجراء |
|---|---|---|---|
| 1 | MSSQL `sa` password | deploy/restore + dev-tooling + CI | ROTATE |
| 2 | PostgreSQL app user | setup_server.sh + deploy_web.sh | ROTATE |
| 3 | `JWT_SECRET` | deploy_web.sh + runbooks | ROTATE |
| 4 | `SESSION_SECRET` | deploy_web.sh + redeploy_new.sh | ROTATE |
| 5 | E2E test password | `.env.example` (placeholder) | ROTATE/ENV |

**الخطوات** (مالك فقط — خارج صلاحياتي):
1. توليد secrets جديدة: `openssl rand -base64 48` × 5
2. تحديث vault / `.env` على الخادم
3. `pm2 reload nama-medical-erp` بعد كل تحديث
4. (اختياري لاحقاً) history rewrite للأسرار من Git

**سكك الأمان المعنية**:
- ✅ Rail 1 (hardcoded secrets) — مُعالَج برمجياً؛ التدوير فقط متبقّي
- ✅ Rail 2 (no PHI) — لا علاقة
- ✅ Rail 9 (no secrets in code) — مُعالَج

**الوكلاء المشاركون**: SECURITY (توصيات) + USER (التنفيذ)

---

### 🎯 TASK 3 — P2: GATE 9 (ZATCA UBL XAdES) — خارطة طريق رفع الحظر
**المصدر**: `GATE9_ZATCA_UBL_XADES_BLOCKED_AR.md`

**الوضع**: `zatca_phase2.js` فيه التشفير الحقيقي (ECDSA secp256k1, PIH chain, QR TLV)، لكن UBL يحوي **placeholder صادق** `UNSIGNED-NO-CSID` — وهذا **مُتعمَّد** (لا يمكن إنتاج XML "موقّع" صحيح بدون شهادة CSID من ZATCA).

**المتطلبات لرفع الحظر** (خارج نطاق هذه الجلسة):
1. **شهادة CSID** من ZATCA (Onboarding) — في vault/encrypted
2. **متجهات اختبار ZATCA** الرسمية (أو Fatoora SDK/simulator)
3. **قواعد C14N الرسمية** من ZATCA بالضبط

**الخطوات المؤجلة (لا تنفيذ الآن)**:
- عند توفّر CSID + متجهات: بناء `buildUBLSignatureExtension(...)` (سقالة بنيوية) واختبار ضد متجهات ZATCA

**سكك الأمان المعنية**:
- ✅ Rail 2 (PHI) — لا علاقة
- ✅ Rail 4 (read-only first) — انتظار credentials
- ⚠️ Rail 8 (CSP/JSDoc) — التعليق يجب أن يبقى واضحاً "سقالة غير مُعتمَدة" حتى الاعتماد

**الوكلاء المشاركون**: SECURITY + BACKEND (مؤجّل)

---

## ما لا أقترحه (تم استبعاده عمداً)

| البند | سبب الاستبعاد |
|---|---|
| بناء أقسام جديدة | المشروع به 46 قسم + 16 نوع منشأة، الكل مُغطّى |
| بناء AI orchestrators جديدة | 13 orchestrator مطبّق + شامل |
| بناء موديول حاسبات سريرية | 18 حاسبة + 47 endpoint `/api/phase3/v2` |
| إعادة هيكلة `app.js` (1.7 MB) | خارج scope + مخالف لـ Rail 5 (backwards compat) |
| كتابة كود ZATCA جديد | محظور بدون CSID |

---

## قيود التنفيذ (اعتُمدت من AGENTS.md §2.2)

- **Rail 1**: لن أكتب في `namaweb/`، `migrations/`، `server.js` دون موافقة صريحة
- **Rail 2**: لا أسرار حقيقية في أي ملف
- **Rail 3**: Phase-by-Phase (T1 → T2 → T3 بترتيب الأولوية)
- **Rail 4**: read-only أولاً، هذا التقرير كله read-only
- **Rail 5**: backwards compat لكل تغيير
- **Rail 6**: لا DDL على الإنتاج بدون backup مُعتمد
- **Rail 7**: PHI encryption لا يُمس
- **Rail 8**: CSP report-only (لا enforce)
- **Rail 9**: حساب money/VAT خادمي فقط
- **Rail 10**: audit log hash-chained
- **Rail 11**: fail-closed على missing tenant
- **Rail 12**: لا طباعة أسرار/PHI
- **Rail 13**: Golden Access Rule (Owner/Admin absolute, others specialty-scoped)

---

## Token Budget & التقديرات

| العنصر | التقدير |
|---|---|
| SCANNER فعلاً | ~4,000 tokens (✅ مكتمل) |
| PLANNER فعلاً | ~5,000 tokens (هذا التقرير) |
| TASK 1 (deploy) | ~2,000 tokens (أوامر + تحقق) |
| TASK 2 (owner rotation) | ~1,500 tokens (دليل فقط) |
| TASK 3 (ZATCA - مؤجّل) | ~3,000 tokens عند رفع الحظر |
| QA | ~2,000 tokens للتحقق بعد النشر |
| **الإجمالي** | **~17,500 tokens** (ضمن 200K budget بكثير) |

---

## ⏸️ في انتظار موافقتك

### 3 خيارات:

1. **🟢 موافقة على T1 فقط** (RAG deploy) — نقوم بنشره من جهاز عنده SSH
2. **🟢 موافقة على T1 + T2** — نشر + تدوير أسرار
3. **🔄 تعديل** — استبدال/إضافة مهمة

> **سؤالي**: هل توافق على الخطة كما هي، أم تريد تعديل/إضافة؟
>
> **ملاحظة**: TASK 1 و 2 (T1, T2) **لا يمكن تنفيذهما من بيئتي** (sandbox بدون SSH لـ Hetzner، وتدوير الأسرار من صلاحية المالك فقط). ما أستطيع تقديمه الآن:
> - **T1**: خطوات قابلة للنسخ، + سكريبت تحقق، + قالب إغلاق مُعبَّأ
> - **T2**: قائمة الأسرار + أوامر `openssl` + runbook مُختصر
> - **T3**: وثيقة "ما يلزم لرفع الحظر" (موجودة فعلاً)
>
> 💡 **اقتراح عملي**: أكتب الآن `READY_PROMPT_PACK.md` محدث بـ T1 و T2 بحيث تنفّذها أنت في 30 دقيقة إجمالاً، وأبقى في standby لأي سؤال.

---

## 📂 المخرجات

- ✅ `project_brain/scans/scan-2026-07-24-0053.json` (SCANNER)
- ✅ `project_brain/orchestrator-state.json` (state)
- ✅ `project_brain/plans/plan-2026-07-24-0053.md` (هذا الملف — PLANNER)
- ⏳ `project_brain/agent-messages.json` (سيُحدَّث بعد موافقتك)

---

**🤖 Multi-Agent Builder v5.0** — Phase 2 (PLANNER) ⏸ Awaiting Approval · Phase 3 (Implementation) 🔒 Blocked until user approves
