# P1 — Gate 7: سجل مخاطر DDL/CoA المحاسبي (Risk Register)

> readiness فقط. يغطّي مخاطر التصميم والتنفيذ المستقبلي.

| # | Risk | Severity | Cause | Mitigation | Blocker? |
|---|---|---|---|---|---|
| 1 | تحويل نوع المال (REAL→NUMERIC) | High | أخطاء تقريب/قفل جدول/إعادة كتابة | `ROUND(::numeric,2)`، بروفة على staging، نافذة صيانة، backup قبل | نعم (للإنتاج) |
| 2 | ربط حساب خاطئ في CoA | High | رمز/تعيين خاطئ | جدول الربط + تحقق رموز المحرك العشرة + بروفة الحالات الست | نعم |
| 3 | ترحيل مزدوج (double posting) | Critical | إعادة ترحيل نفس المستند | `uq_journal_idempotency(tenant,source_type,source_id)` + posting_reference | نعم |
| 4 | قيد غير متوازن | Critical | منطق ترحيل خاطئ | `validateBalanced` (28/28) + chk_jl_one_side/nonneg + (اختياري) trigger | نعم |
| 5 | tenant_id مفقود على CoA | High | عدم تماثل المخطط الحالي | ADD tenant_id + UNIQUE(tenant,code) + توصيل بحث بالمستأجر | نعم |
| 6 | تراجع RLS (P0) | Critical | تعديل غير مقصود على جداول/سياسات | DDL هذه لا تلمس RLS؛ تحقق عدم التراجع في البروفة | نعم |
| 7 | انتهاك FK | Med | بيانات يتيمة قائمة | `validate.sql` قبل إضافة FK؛ توقف عند > 0 | نعم |
| 8 | فواتير قديمة غير مرحَّلة | Med | المحرك جديد/غير موصول | قرار backfill منفصل لاحقاً (لا في هذه المرحلة) | لا |
| 9 | تعقيد الاسترجاع | Med | down يخفض النوع (فقد دقة) | اعتماد backup restore كمسار أساسي للاسترجاع | لا |
| 10 | عدم تطابق التقارير | Med | اختلاف تجميع الإيراد (4000 مجمّع مقابل تفصيل) | توثيق التجميع الحالي + تفصيل لاحق | لا |
| 11 | معالجة VAT | High | حساب/عكس ضريبة خاطئ | `splitVatInclusive` 15% شامل + اختبارات؛ ZATCA توقيع خارج النطاق | لا (للمرحلة) |
| 12 | معالجة ذمم التأمين | High | رفض يحذف الإيراد صامتاً | قاعدة: الرفض يحوّل الذمة بقيد صريح (1110→1100/4090)؛ بانٍ مستقبلي | لا (للمرحلة) |
| 13 | الإنتاج لا ينشئ الجداول تلقائياً | High | `NODE_ENV=production` يتخطّى init | إنشاء/تأكيد الجداول صراحةً في preflight الإنتاج | نعم (للإنتاج) |
| 14 | فجوات بناة المحرك (6 عمليات) | Med | دوال build* ناقصة | موثّقة في Gate 4؛ تُضاف بعد جاهزية DDL/CoA | لا |

## مخاطر متابعة مُسجَّلة (خارج النطاق)
- `REGISTER_GITMODULES_FOR_NAMAWEB_SUBMODULE` (Med) — gitlink يتيم.
- `REVIEW_DF893AB_SECURITY_HARDENING_DELTA` (Med) — مراجعة عمل أمني في فرع namaweb متباعد.

## المخرج التالي
Gate 8: الإغلاق + commit/push.
