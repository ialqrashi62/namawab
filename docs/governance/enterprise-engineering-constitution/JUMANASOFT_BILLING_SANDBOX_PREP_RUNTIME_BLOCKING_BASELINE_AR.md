# تأكيد قيود وحظر تشغيل مرحلة إعداد الفوترة المستندية (Jumanasoft Billing Sandbox Prep Runtime Blocking Baseline)

* **المشروع:** منصة نما الطبية (NamaMedical ERP)
* **المرحلة:** إعداد بوابات الدفع التجريبية مستندياً (PHASE_BILLING_SANDBOX_PREP_DOCS_ONLY)
* **البوابة:** البوابة 0.4 — خط أساس حظر التشغيل (Gate 0.4 — Runtime Blocking Baseline)
* **الحالة:** تم التوثيق والفرض بنجاح (ENFORCED) ✅

---

## 1. ثوابت وحظر وضع التشغيل الحالي للمرحلة

يتم فرض الحظر التام والمطلق على كافة أنشطة الفوترة الخارجية والمخططات طوال هذه المرحلة وفق المحددات التالية:

* **production touched:** NO ❌ (لمس أو الاتصال ببيئة الإنتاج محظور تماماً).
* **SQL executed:** NO ❌ (تنفيذ أو تشغيل أي استعلامات SQL لتعديل البيانات محظور).
* **DDL executed:** NO ❌ (إنشاء جداول أو تشغيل DDL محظور تماماً).
* **e47 executed:** NO ❌ (تشغيل ملفات هجرة جداول الفوترة `e47` محظور تماماً).
* **provider sandbox:** NO ❌ (ربط أو تفعيل بوابات دفع تجريبية محظور).
* **provider live:** NO ❌ (الاتصال بموفري الدفع الحيين محظور كلياً).
* **checkout live:** NO ❌ (عمليات الدفع والتسجيل الحية معطلة).
* **webhook live:** NO ❌ (استقبال إشعارات الدفع الحية من موفري الخدمة محظور).
* **external HTTP:** NO ❌ (إجراء استدعاءات HTTP خارجية لبوابات الدفع محظور تماماً).
* **billing activation:** NO ❌ (تفعيل بوابات الفوترة حية معطل كلياً).
* **observe/enforce:** NO ❌ (معطل بالكامل).

---
**الالتزام:** تظل هذه المرحلة مقتصرة بالكامل على الصياغة التوثيقية والتحليل الساكن، لمنع أي انزلاق أمني أو تشغيلي في النظام.
