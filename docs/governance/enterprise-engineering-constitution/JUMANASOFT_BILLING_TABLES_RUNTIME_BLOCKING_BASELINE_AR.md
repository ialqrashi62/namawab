# تقرير خط أساس حظر تشغيل جداول الفوترة (Jumanasoft Billing Tables Runtime Blocking Baseline)

* **المشروع:** منصة نما الطبية (NamaMedical ERP)
* **المرحلة:** تصميم جداول الفوترة المرشحة (PHASE_BILLING_TABLES_CANDIDATE_DESIGN)
* **البوابة:** البوابة 0.3 — خط أساس حظر تشغيل الجداول (Gate 0.3 — Runtime Blocking Baseline)
* **الحالة:** تم التوثيق والفرض بنجاح (ENFORCED) ✅

---

## 1. قيود وحظر وضع التشغيل الحالي للجداول

يتم فرض الحظر التام على تشغيل أو تنفيذ أي هجرات أو تعديل هيكلي في قاعدة البيانات طوال هذه المرحلة وفق المحددات أدناه:

* **production deploy:** NO ❌ (النشر لبيئة الإنتاج محظور تماماً).
* **public staging:** NO ❌ (الاستضافة العامة معطلة تماماً).
* **observe:** NO ❌ (معطل).
* **enforce:** NO ❌ (معطل بالكامل).
* **billing activation:** NO ❌ (تفعيل عمليات الفوترة معطل).
* **provider live:** NO ❌ (الاتصال بموفري الدفع المباشر محظور).
* **checkout live:** NO ❌ (عمليات الدفع والتسجيل الحية معطلة).
* **webhook live:** NO ❌ (استقبال إشعارات الدفع الحية معطل).
* **DDL execution:** NO ❌ (تشغيل DDL أو إنشاء جداول في قاعدة البيانات محظور تماماً).
* **migration execution:** NO ❌ (تشغيل ملفات الهجرات SQL محظور كلياً).

---
**الالتزام:** تظل ملفات هجرة جداول الفوترة كنسخة مرشحة مستندية فقط (Candidate Design) ويمنع تشغيلها كلياً على أي قاعدة بيانات حالياً.
