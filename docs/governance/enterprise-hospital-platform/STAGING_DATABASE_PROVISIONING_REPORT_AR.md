# تقرير تهيئة وعزل قاعدة بيانات الاختبار (Staging Database Provisioning Report)

* **المشروع:** منصة نما الطبية (NamaMedical ERP)
* **المرحلة:** تجهيز بيئة الاختبار وجمع الشواهد (PHASE_OWNER_DEVOPS_STAGING_PROVISIONING_AND_EVIDENCE_CAPTURE)
* **البوابة:** البوابة 3 — عزل قاعدة البيانات (Gate 3 — Staging Database Provisioning)
* **الحالة:** تم التجهيز والتحقق بنجاح (SUCCESS) ✅

---

## 1. مواصفات قاعدة البيانات المعزولة

تم إنشاء وتهيئة قاعدة بيانات معزولة ومستقلة تماماً لاستخدامها في بيئة الاختبار دون أي تداخل مع قاعدة بيانات الإنتاج الفعلي:
* **اسم قاعدة بيانات الاختبار:** `jumanasoft_staging`
* **المستخدم المالك لقاعدة الاختبار:** `jumanasoft_staging_user`
* **صلاحيات المستخدم المالك:**
  * `SUPERUSER`: لا (false) ❌
  * `BYPASSRLS`: لا (false) ❌
  * `CREATEDB`: لا (false) ❌
  * `CREATEROLE`: لا (false) ❌
* **عزل قاعدة الإنتاج (`nama_medical_web`):**
  * تم إلغاء كافة صلاحيات الاتصال أو الاستعلام للمستخدم `jumanasoft_staging_user` على قاعدة بيانات الإنتاج لحماية البيانات الحية.

---

## 2. استعادة الهيكل وتطبيق الهجرة (e25)

1. تم أخذ نسخة احتياطية للقراءة فقط من قاعدة بيانات الإنتاج واستعادتها بنجاح داخل قاعدة بيانات الاختبار لإنشاء الهيكل الأساسي للبيانات الوهمية.
2. تم منح صلاحيات `USAGE` و `SELECT, INSERT, UPDATE, DELETE` للمستخدم المالك على كافة الجداول.
3. تم تطبيق الهجرة المخصصة للخطط والتسعير [e25_plans_pricing_candidate_up.sql](file:///c:/Users/ice/Desktop/NamaMedical/namaweb/migrations/e25_plans_pricing_candidate_up.sql) واجتياز كود التحقق بنجاح 100%.

---
**القرار:** قاعدة البيانات مهيأة ومعزولة بالكامل وجاهزة للفحص.
