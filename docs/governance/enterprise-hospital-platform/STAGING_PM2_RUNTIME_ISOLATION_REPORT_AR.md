# تقرير عزل عملية التشغيل لبيئة الاختبار (Staging PM2 & Runtime Process Isolation Report)

* **المشروع:** منصة نما الطبية (NamaMedical ERP)
* **المرحلة:** تجهيز بيئة الاختبار وجمع الشواهد (PHASE_OWNER_DEVOPS_STAGING_PROVISIONING_AND_EVIDENCE_CAPTURE)
* **البوابة:** البوابة 5 — عزل عملية التشغيل (Gate 5 — PM2/Runtime Process Isolation)
* **الحالة:** تم التحقق والتوثيق بنجاح (SUCCESS) ✅

---

## 1. مواصفات عملية التشغيل المستقلة

تم عزل تشغيل التطبيق في بيئة Staging تحت عملية مستقلة بالكامل تضمن عدم تأثر خادم الإنتاج بأي عمليات إعادة تشغيل أو فحص:
* **اسم العملية المعتمد (PM2 Process Name):** `nama-app-staging`
* **منفذ الاستماع (Listening Port):** `3010` (معزول تماماً عن منفذ الإنتاج `3005`).
* **مسارات السجلات المخصصة (Isolated Logs):**
  * سجل المخرجات: `/var/log/nama-app-staging/out.log`
  * سجل الأخطاء: `/var/log/nama-app-staging/err.log`
* **فصل العمليات:** تم التحقق من عدم مشاركة أي مسارات للملفات المؤقتة أو الجلسات بين عملية الإنتاج وعملية الاختبار.

---
**القرار:** عملية التشغيل معزولة بالكامل وتعمل بأمان.
