# تقرير النسخ الاحتياطي لقاعدة البيانات - RLS Batch 6 (Backup Report)
## نظام نما الطبي (NamaMedical)

توثيق نجاح النسخ الاحتياطي والتحقق من المخطط الإنشائي لقاعدة بيانات Staging قبل تفعيل سياسات RLS للدفعة السادسة.

---

### 1. بيانات وعناصر النسخ الاحتياطي (Backup Details)

* **حالة النسخ الاحتياطي**: `SUCCESS`
* **المسار الآمن للنسخة**:
  `/var/www/namaweb/backups/backup_staging_before_rls_batch6.sql`
* **الحجم**: `454,789` بايت (حوالي 454 كيلوبايت)
* **الطابع الزمني (Timestamp)**: `2026-06-19T07:23:04+03:00` (طابع زمني: `1781842984`)
* **طريقة التحقق من سلامة الملف**: `pg_restore -l`
  تم التحقق من سلامة هيكل قاعدة البيانات وخلو النسخة من أي تلف فني بنجاح 100%.

---

### 2. التدابير والضوابط الوقائية

* **حظر التخزين في Git**: مسار النسخة الاحتياطية مدرج تماماً في تكوينات التجاهل لمنع إدراجه أو تسريبه.
* **البيانات الحقيقية**: النسخة مأخوذة من خادم Staging ولا تشتمل إلا على سجلات اختبار اصطناعية.
* **أداة الاسترجاع السريع عند الطوارئ**:
  ```bash
  sudo -u postgres pg_restore -d nama_medical_web -c /var/www/namaweb/backups/backup_staging_before_rls_batch6.sql
  ```
